# Brief: 10, ordo-init restyled to the skill layout, with its rule inventory

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `docs/dev/skill-layout.md`, every ruling in `.scratch/1-one-layout-for-every-skill/plan.md` (a row may name one heading line), and `skills/spec/SKILL.md` and `skills/land/SKILL.md` as landed.

## What is on the tree (read on main at aa7cfe2)

- `skills/ordo-init/SKILL.md` is 57 lines, version 1.0.0, with the sections What it reads, Drafting the file, Ignore rules, Approval then writing, Checking an existing file, Rules.
- `python3 utils/check_skill_layout.py skills/ordo-init` prints 9 error lines.
- `skills/repo-setup/SKILL.md` describes running `/ordo-init` (its step 5); no file names a section of ordo-init (`grep -rn`).

## What to build

1. `skills/ordo-init/SKILL.md` rewritten to `docs/dev/skill-layout.md`, version 1.1.0.
2. `.scratch/1-one-layout-for-every-skill/inventories/ordo-init.md`: one row per rule of the old file, `- Old:` at `aa7cfe2`.

## What it must do

- Every rule of the old file keeps its meaning, each written once, one rule per bullet, item or row.
- The drafting rules for each key, the ignore-rule checks and their commands, the order of what is shown for approval, the check run after writing, and the check mode keep their meaning exactly.

## Verify before you report

From the worktree's root: `python3 utils/check_skill_layout.py skills/ordo-init` prints `ok:`; `python3 utils/check_rule_inventory.py .scratch/1-one-layout-for-every-skill/inventories/ordo-init.md` prints `ok:`; every command of the verify list passes.

## Report

Write it to `.scratch/1-one-layout-for-every-skill/agents/reviews/10-report.md`, in the end state, with every changed file's line count, the grep for removed section names with its output, and the before and after of what a user reads differently.
