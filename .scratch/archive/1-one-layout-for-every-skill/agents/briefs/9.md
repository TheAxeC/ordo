# Brief: 9, plan-help restyled to the skill layout, with its rule inventory

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `docs/dev/skill-layout.md`, every ruling in `.scratch/1-one-layout-for-every-skill/plan.md`, and `skills/spec/SKILL.md` as landed.

## What is on the tree (read on main at 0fa6d65)

- `skills/plan-help/SKILL.md` is 47 lines, version 1.5.0, with the sections The sequence, printed verbatim, and The position, for `<entry>`.
- `python3 utils/check_skill_layout.py skills/plan-help` prints 9 error lines.
- The README says the full sequence is what `/plan-help` prints (`grep -n 'plan-help' README.md`).

## What to build

1. `skills/plan-help/SKILL.md` rewritten to `docs/dev/skill-layout.md`, version 1.6.0.
2. `.scratch/1-one-layout-for-every-skill/inventories/plan-help.md`: one row per rule of the old file, `- Old:` at `0fa6d65`.

## What it must do

- Every rule of the old file keeps its meaning, each written once, one rule per bullet, item or row.
- The printed sequence is the skill's output: it stays byte for byte as it is, in its own section, and the step that prints it names that section.

## Verify before you report

From the worktree's root: `python3 utils/check_skill_layout.py skills/plan-help` prints `ok:`; `python3 utils/check_rule_inventory.py .scratch/1-one-layout-for-every-skill/inventories/plan-help.md` prints `ok:`; the printed block is byte-identical to the old one (a diff of the two blocks prints nothing); every command of the verify list passes.

## Report

Write it to `.scratch/1-one-layout-for-every-skill/agents/reviews/9-report.md`, in the end state, with every changed file's line count, the grep for removed section names with its output, and the before and after of what a user reads differently.
