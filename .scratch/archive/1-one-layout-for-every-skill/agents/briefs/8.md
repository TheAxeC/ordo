# Brief: 8, land restyled to the skill layout, with its rule inventory

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `docs/dev/skill-layout.md`, every ruling in `.scratch/1-one-layout-for-every-skill/plan.md`, and `skills/refute/SKILL.md` and `skills/spec/SKILL.md` as landed.

## What is on the tree (read on main at e643b34)

- `skills/land/SKILL.md` is 41 lines, version 1.5.0, with the sections What it requires, What it does in order (steps 1 to 11 and 5a), The landing script, Rules; three of its sentences were carried to the user's booked-list ruling in step 7.
- `python3 utils/check_skill_layout.py skills/land` prints 9 error lines.
- 1 lines outside the skill cite it by its step numbers or its templates (`grep -rn`), among them plan-orchestration's "`/land` produces it at its step 8"; land's own text refers to its steps 5, 9 and 11. The step numbers 1 to 11 stay; step 5a, the look, becomes the reference section "The look", which step 5 names.

## What to build

1. `skills/land/SKILL.md` rewritten to `docs/dev/skill-layout.md`, version 1.6.0.
2. `.scratch/1-one-layout-for-every-skill/inventories/land.md`: one row per rule of the old file, `- Old:` at `e643b34`.

## What it must do

- Every rule of the old file keeps its meaning, each written once, one rule per bullet, item or row.
- The cherry-pick range, the restore of ledger files, the red-line handling with its ask-list commands, the A/B figures, the booking's contents, the commit by explicit path with its check afterwards, and the landing report keep their meaning exactly.

## Verify before you report

From the worktree's root: `python3 utils/check_skill_layout.py skills/land` prints `ok:`; `python3 utils/check_rule_inventory.py .scratch/1-one-layout-for-every-skill/inventories/land.md` prints `ok:`; every command of the verify list passes, `land.test.sh` among them.

## Report

Write it to `.scratch/1-one-layout-for-every-skill/agents/reviews/8-report.md`, in the end state, with every changed file's line count, the grep for removed section names over skills, utils, docs and README.md with its output, and the before and after of what a user of `/land` reads differently.
