# Brief: 12, plan-retro restyled to the skill layout, with its rule inventory

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `docs/dev/skill-layout.md`, every ruling in `.scratch/1-one-layout-for-every-skill/plan.md`, and `skills/roadmap/SKILL.md` and `skills/ordo-init/SKILL.md` as landed.

## What is on the tree (read on main at 9eda91c)

- `skills/plan-retro/SKILL.md` is 53 lines, version 1.0.0, with the sections What it reads, Grouping, The proposal for a recurring kind, What it writes, Rules.
- `python3 utils/check_skill_layout.py skills/plan-retro` prints 9 error lines.
- Its templates, `collect_findings.py` with its test and `retro.md`, stay as they are.

## What to build

1. `skills/plan-retro/SKILL.md` rewritten to `docs/dev/skill-layout.md`, version 1.1.0.
2. `.scratch/1-one-layout-for-every-skill/inventories/plan-retro.md`: one row per rule of the old file, `- Old:` at `9eda91c`.

## What it must do

- Every rule of the old file keeps its meaning, each written once, one rule per bullet, item or row; the collector command, the recurring threshold, the proposal order and the approval of each proposal before any edit keep their meaning exactly.

## Verify before you report

From the worktree's root: `python3 utils/check_skill_layout.py skills/plan-retro` prints `ok:`; `python3 utils/check_rule_inventory.py .scratch/1-one-layout-for-every-skill/inventories/plan-retro.md` prints `ok:`; every command of the verify list passes.

## Report

Write it to `.scratch/1-one-layout-for-every-skill/agents/reviews/12-report.md`, in the end state, with every changed file's line count, the grep for removed section names with its output, and the before and after of what a user reads differently.
