# Brief: 2, the launch_note key in every place that lists the optional keys

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `docs/launch-note.md`, `skills/plan/SKILL.md`, `skills/plan/templates/plan.yaml`, `skills/plan/templates/plan.projects.yaml`, `skills/plan/templates/orchestrator-state.md`, `skills/ordo-init/SKILL.md`, `skills/ordo-init/templates/check_config.py` and its test, `skills/land/templates/land.test.sh` (its check of the example files against the state template), `README.md`, and the rulings in `.scratch/2-a-launch-notes-for-builders-run-as-their-own-process/plan.md`.

## What is on the tree (read on main at 5e9ec86)

- `git grep -n workers_at_once -- ':!.scratch'` lists the places that name the optional keys: `skills/plan/SKILL.md:52` (the configuration block's key list), `skills/plan/templates/plan.yaml:21`, `skills/plan/templates/plan.projects.yaml:23, 42`, `skills/plan/templates/orchestrator-state.md:21`, and the orchestration rules in `skills/plan-orchestration/SKILL.md` (not a key list). `skills/ordo-init/SKILL.md:62` names `bench` and `look` as left out without a reason; `README.md:96` describes the optional keys in general.
- `check_config.py` learns the keys, their defaults and the required ones from `skills/plan/templates/plan.yaml` (`example_keys`), so a key missing from that file is an unknown key; it checks a value's type against its default's.
- `land.test.sh` checks that both example files carry exactly the keys of the state template's configuration block, each marked required or optional with its default.

## What to build

1. `launch_note: ""` in `plan.yaml` after `bench`, with the comment `# optional, default "". The absolute path of the command that records a builder started as its own process (docs/launch-note.md); "" means none is recorded.`; the same key in both projects of `plan.projects.yaml`; `launch_note:` in the state template's configuration block with a comment in its style.
2. `skills/plan/SKILL.md`: `launch_note` in the configuration block's key list.
3. `skills/ordo-init/SKILL.md`: `launch_note` left out unless the user names a recording command.
4. `README.md`: the optional keys' paragraph names `launch_note` and points at `docs/launch-note.md`.
5. `check_config.py`: a non-empty `launch_note` must be an absolute path to an executable file, else an error naming the value; `check_config.test.sh` gains cases for a set, valid key (passes), a relative path, a missing file and a file that is not executable (each an error).

## What it must do

- `sh skills/land/templates/land.test.sh` and `sh skills/ordo-init/templates/check_config.test.sh` pass with the key in the templates.
- The files follow the change standard and the prose standard: ASCII, no history.

## Verify before you report

From the worktree's root: both tests above pass; each new check_config case turns red with its check removed (planted and reverted, the output quoted); every command of the verify list passes, and `sh skills/plan-orchestration/templates/launch.test.sh` still passes.

## Report

Write it to `.scratch/2-a-launch-notes-for-builders-run-as-their-own-process/agents/reviews/2-report.md`, in the end state: the DONE table with the proof of each item, the planted failures' output, and the verify list's output.
