# Brief: 4, launch.test.sh joins the documented checks and the verify list

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `README.md` (its "Tests" section), `docs/dev/building.md`, `skills/repo-setup/templates/docs/dev/prose-standard.md`, `skills/plan-orchestration/templates/launch.test.sh`, and the rulings in `.scratch/2-a-launch-notes-for-builders-run-as-their-own-process/plan.md`.

## What is on the tree (read on main at 916a144)

- `grep -n check_coverage.test.sh README.md docs/dev/building.md docs/dev/change-standard.md .scratch/2-a-launch-notes-for-builders-run-as-their-own-process/orchestrator-state.md` finds the four test lists: the README's code block (line 112) with one bullet per test after it (line 122), `docs/dev/building.md:13`, `docs/dev/change-standard.md:49`, and the state file's `verify:` block (line 14). None of them names `launch.test.sh`.
- The skills' tests come first in each list (land, check_config, collect_findings, sync_rules), then the utils tests.
- `sh skills/plan-orchestration/templates/launch.test.sh` prints `PASS: launch.sh scratch tests`; its header comment names its cases.

## What to build

1. `README.md`: `sh skills/plan-orchestration/templates/launch.test.sh` in the Tests code block after `sync_rules.test.sh`, and a bullet after the `sync_rules.test.sh` bullet saying what the test checks, from its header comment.
2. `docs/dev/building.md`: the same command after `sync_rules.test.sh`, with a short comment in the style of the list.
3. `docs/dev/change-standard.md`: the same command with the filter `2>&1 | tail -1`, after `sync_rules.test.sh`.
4. The state file's `verify:` block: the filtered command after `sync_rules.test.sh`. The ledger is written on main at landing, so this item is made there.

## What it must do

- Every list names the same tests in the same order.
- The files follow the change standard and the prose standard: ASCII, no history.

## Verify before you report

From the worktree's root: every command of the verify list passes, and `sh skills/plan-orchestration/templates/launch.test.sh 2>&1 | tail -1` prints `PASS: launch.sh scratch tests`; the lists compared with `grep -n 'test.sh' README.md docs/dev/building.md docs/dev/change-standard.md`, quoted.

## Report

Write it to `.scratch/2-a-launch-notes-for-builders-run-as-their-own-process/agents/reviews/4-report.md`, in the end state: the DONE table with the proof of each item and the verify list's output.
