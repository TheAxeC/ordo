# Brief: 6, spec restyled to the skill layout, with its rule inventory

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `docs/dev/skill-layout.md`, the rulings in `.scratch/1-one-layout-for-every-skill/plan.md` (the required tables restate nothing; frontmatter-only lines map to Quick start), and `skills/plan/SKILL.md` as landed in step 5.

## What is on the tree (read on main at a682c14)

- `skills/spec/SKILL.md` is 54 lines, version 1.3.0, with the sections What it reads, What it writes, Preflight before any write, When it stopped, Rules.
- `python3 utils/check_skill_layout.py skills/spec` prints 8 error lines.
- Only the spec skill's `templates/brief.md` names a term of the file ("Decisions taken in this brief"); no file names its sections (`grep -rn` for the section names over skills, README.md and docs).

## What to build

1. `skills/spec/SKILL.md` rewritten to `docs/dev/skill-layout.md`, version 1.4.0.
2. `.scratch/1-one-layout-for-every-skill/inventories/spec.md`: one row per rule of the old file, `- Old:` at `a682c14`.

## What it must do

- Every rule of the old file keeps its meaning, each written once, one rule per bullet, item or row.
- The ruling text a user types, the booking of a ruling, the worktree command, the dispatch block's fields and the brief's contents keep their meaning exactly.

## Verify before you report

From the worktree's root: `python3 utils/check_skill_layout.py skills/spec` prints `ok:`; `python3 utils/check_rule_inventory.py .scratch/1-one-layout-for-every-skill/inventories/spec.md` prints `ok:`; every command of the verify list passes.

## Report

Write it to `.scratch/1-one-layout-for-every-skill/agents/reviews/6-report.md`, in the end state.
