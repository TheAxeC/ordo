# Brief: 13, repo-setup restyled to the skill layout, with its rule inventory

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `docs/dev/skill-layout.md`, every ruling in `.scratch/1-one-layout-for-every-skill/plan.md`, and `skills/ordo-init/SKILL.md` and `skills/plan-retro/SKILL.md` as landed.

## What is on the tree (read on main at 709fcf6)

- `skills/repo-setup/SKILL.md` is 92 lines, version 1.0.0, with an invocation block and the sections What it asks, The tree, Order of work, sync, Rules.
- `python3 utils/check_skill_layout.py skills/repo-setup` prints 10 error lines.
- The tree block names "question 6" and "question 3"; the question numbers stay. The rename to `scaffold` is roadmap entry 11, not this step; the name stays.

## What to build

1. `skills/repo-setup/SKILL.md` rewritten to `docs/dev/skill-layout.md`, version 1.1.0; the setup as numbered steps, sync as a numbered subsection of Steps.
2. `.scratch/1-one-layout-for-every-skill/inventories/repo-setup.md`: one row per rule of the old file, `- Old:` at `709fcf6`.

## What it must do

- Every rule of the old file keeps its meaning, each written once, one rule per bullet, item or row, one action per numbered item; nothing written before the user approves; the install command, the checks and the sync exit statuses keep their meaning exactly.

## Verify before you report

From the worktree's root: `python3 utils/check_skill_layout.py skills/repo-setup` prints `ok:`; `python3 utils/check_rule_inventory.py .scratch/1-one-layout-for-every-skill/inventories/repo-setup.md` prints `ok:`; every command of the verify list passes.

## Report

Write it to `.scratch/1-one-layout-for-every-skill/agents/reviews/13-report.md`, in the end state, with every changed file's line count, the verify lines quoted, the grep for removed section names with its output, and the before and after of what a user reads differently.
