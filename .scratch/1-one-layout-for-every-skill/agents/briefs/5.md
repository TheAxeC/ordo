# Brief: 5, plan restyled to the skill layout, with its rule inventory

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `docs/dev/skill-layout.md`, and `skills/plan-orchestration/SKILL.md` as landed in step 4, the first skill in the layout.

## What is on the tree (read on main at ea8d02d)

- `skills/plan/SKILL.md` is 33 lines, version 1.6.0, with the sections What it reads, What it writes, Rules.
- `python3 utils/check_skill_layout.py skills/plan` prints 6 error lines.
- 6 other skills refer to "its required keys and defaults as `/plan` states them" (`grep -rln`); the new file keeps stating the keys, the required ones and the defaults where those references can find them.

## What to build

1. `skills/plan/SKILL.md` rewritten to `docs/dev/skill-layout.md`, version 1.7.0.
2. `.scratch/1-one-layout-for-every-skill/inventories/plan.md`: one row per rule of the old file, `- Old:` at `ea8d02d`, frontmatter-only lines mapped to Quick start (plan.md's ruling).

## What it must do

- Every rule of the old file keeps its meaning, each written once, one rule per bullet or item.
- What the step list is drafted from, the closing step `/plan` writes itself, the slug example, the refusals, and the executor default keep their wording's meaning.

## Verify before you report

From the worktree's root: `python3 utils/check_skill_layout.py skills/plan` prints `ok:`; `python3 utils/check_rule_inventory.py .scratch/1-one-layout-for-every-skill/inventories/plan.md` prints `ok:`; every command of the verify list passes.

## Report

Write it to `.scratch/1-one-layout-for-every-skill/agents/reviews/5-report.md`, in the end state: the checks' output verbatim, the files with line counts, the sections before and after, the judgment calls.
