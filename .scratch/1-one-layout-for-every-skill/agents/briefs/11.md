# Brief: 11, roadmap restyled to the skill layout, with its rule inventory

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `docs/dev/skill-layout.md`, every ruling in `.scratch/1-one-layout-for-every-skill/plan.md`, and `skills/ordo-init/SKILL.md` and `skills/land/SKILL.md` as landed.

## What is on the tree (read on main at 19d395a)

- `skills/roadmap/SKILL.md` is 71 lines, version 1.0.0, with an invocation block and the sections What it reads, The format is the file's, A capability map beside the ordered file, add, move done drop, Writing, Rules.
- `python3 utils/check_skill_layout.py skills/roadmap` prints 11 error lines.
- 7 lines outside the skill name `/roadmap add`, `/roadmap done` or the roadmap skill's template (`grep -rn`); the invocations and `templates/roadmap.md` stay as they are.

## What to build

1. `skills/roadmap/SKILL.md` rewritten to `docs/dev/skill-layout.md`, version 1.1.0; each command (show, add, move, done, drop) a numbered subsection of Steps.
2. `.scratch/1-one-layout-for-every-skill/inventories/roadmap.md`: one row per rule of the old file, `- Old:` at `19d395a`.

## What it must do

- Every rule of the old file keeps its meaning, each written once, one rule per bullet, item or row; the rules the roadmap file's introduction states keep binding the skill; entry numbers are never renumbered; nothing is written before approval.

## Verify before you report

From the worktree's root: `python3 utils/check_skill_layout.py skills/roadmap` prints `ok:`; `python3 utils/check_rule_inventory.py .scratch/1-one-layout-for-every-skill/inventories/roadmap.md` prints `ok:`; every command of the verify list passes.

## Report

Write it to `.scratch/1-one-layout-for-every-skill/agents/reviews/11-report.md`, in the end state, with every changed file's line count, the grep for removed section names with its output, and the before and after of what a user reads differently.
