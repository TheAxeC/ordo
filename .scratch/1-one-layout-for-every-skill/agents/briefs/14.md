# Brief: 14, the layout check wired in as a repository check

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `docs/dev/skill-layout.md`, `docs/dev/building.md`, the README's Tests section, and every ruling in `.scratch/1-one-layout-for-every-skill/plan.md`.

## What is on the tree (read on main at fe1f5e7)

- `python3 utils/check_skill_layout.py` with no argument checks every `skills/*/SKILL.md` and prints ten `ok:` lines, exit 0 (run on main at fe1f5e7).
- `docs/dev/building.md`'s command block, the command block under "Commands and their filters" in `docs/dev/change-standard.md`, the `verify` list of `orchestrator-state.md` and the README's Tests block hold the seven tests (and, except the README, the ASCII check); none runs the layout check itself (`grep -n "check_skill_layout" README.md docs/dev/*.md` finds only the test line and the skill-layout page).
- `docs/dev/change-standard.md`, "Read before changing anything" item 2, says "Ordo has no standards page yet"; `docs/dev/skill-layout.md` is one, and it is the one `standards:` in the state file lists.
- `npx skills add . --list` prints `Found 10 skills`.

## What to build

1. `docs/dev/building.md`: `python3 utils/check_skill_layout.py` in the command block, after the tests and before the ASCII check, with a comment naming what it checks; a paragraph stating its pass (exit 0, one `ok:` line per skill, an error printed as `<path>:<line>: <what is wrong>`) and that it takes no filter.
2. `docs/dev/change-standard.md`: the same command in the "Commands and their filters" block, in the same place; item 2 of "Read before changing anything" names `docs/dev/skill-layout.md` as the standard for every `skills/*/SKILL.md` instead of saying there is none.
3. `README.md` Tests: the check named as a check of the skills themselves, with the command, beside the tests.
4. The `verify` list in `orchestrator-state.md`: the same command, in the same place (the orchestrator's file; written on main at landing, since the ledger is written only on main).

## What it must do

- The layout check runs from the repository root over every `skills/*/SKILL.md`, in every place the verify commands are listed, and passes on the tree.
- No sentence in README.md, `docs/` or a skill is left false: grep `check_skill_layout`, `standards page` and `the seven` across `skills/`, `utils/`, `docs/` and `README.md`.

## Verify before you report

From the worktree's root: every command of `docs/dev/building.md` as changed passes (the seven `PASS:` lines, ten `ok:` lines, the ASCII check prints nothing); `npx skills add . --list 2>&1 | grep Found` prints `Found 10 skills`. The proof that the new line checks something: a copy of the tree with one skill's `## Rules` heading renamed makes it exit 1, and the report quotes that red.

## Report

Write it to `.scratch/1-one-layout-for-every-skill/agents/reviews/14-report.md`, in the end state, with every changed file's line count, the verify lines quoted, the grep with its output, the red run quoted, and the before and after of what a reader of each page sees.
