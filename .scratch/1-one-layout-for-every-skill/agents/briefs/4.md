# Brief: 4, plan-orchestration restyled to the skill layout, with its rule inventory

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `docs/dev/skill-layout.md`.

## What is on the tree (read on main at 836f5c5)

- `skills/plan-orchestration/SKILL.md` is 96 lines (`wc -l`), version 2.6.1, with twelve `##` sections: The two tiers, and the harnesses; Handing the plan from one orchestrator to another; The loop, one step at a time; The review, earned; The recurring-findings pass; Two steps in flight; Launching a builder; What earns a step of its own; Stops; Reports; Usage; The pace when a deadline is set.
- `python3 utils/check_skill_layout.py skills/plan-orchestration` prints 21 error lines and exits 1.
- `utils/check_rule_inventory.py` reads an inventory at `.scratch/1-one-layout-for-every-skill/inventories/<skill>.md` whose old file is `git show 836f5c5tree at v1.0.0; this step changes only the tree's copy.

## What to build

1. `skills/plan-orchestration/SKILL.md` rewritten to `docs/dev/skill-layout.md`: the frontmatter, the title and paragraph, Quick start, Use instead, What it reads, Steps (the loop), reference sections (the tiers and harnesses, resuming and handing over, the review earned, the recurring-findings pass, two steps in flight, launching a builder, what earns a step, reports, usage, the pace under a deadline), Stops, Anti-patterns, Rules. `metadata.version` becomes 2.7.0.
2. `.scratch/1-one-layout-for-every-skill/inventories/plan-orchestration.md`: one row per rule of the old file, as `utils/check_rule_inventory.py` defines the format, with `- Old:` at `836f5c5`.

## What it must do

- Every rule of the old file keeps its meaning in the new file, qualifiers included: the one-round exception to `repair_rounds`, the executor choice (manuscript content always `academic-paper`), the resumption checks per harness, the launch commands verbatim, the usage row's window, the night rule.
- A rule is written once; another place that needs it names its section.
- The frontmatter's description names no neighbouring skill, by the layout; the invocations it describes are named in the body.
- Nothing is added that the old file does not state, except the Quick start, Use instead and Anti-patterns content the layout requires, each drawn from rules the old file states or from the neighbouring skills' own descriptions.

## Decisions taken in this brief (each reversible, none silent)

1. A frontmatter line of the old file maps to the body section that states the same rule, since the inventory's places are sections: the name and triggers to Quick start, the description's parts to the steps and sections that carry them, the version to Rules with the new version.
2. The loop's items keep their numbers 1 to 10, so references to "step 6" and "steps 3 to 9" elsewhere stay true; rules inside a loop item become nested bullets under it.

## Verify before you report

Run from the worktree's root:

1. `python3 utils/check_skill_layout.py skills/plan-orchestration` prints `ok: skills/plan-orchestration/SKILL.md`.
2. `python3 utils/check_rule_inventory.py .scratch/1-one-layout-for-every-skill/inventories/plan-orchestration.md` prints `ok:` (the inventory is written in the worktree's copy of the ledger path, and the orchestrator copies it to main's ledger at landing).
3. Every command of the verify list passes.
4. `grep -rn 'plan-orchestration' --include=SKILL.md skills README.md docs`: each section the other files name in plan-orchestration (for example "Launching a builder", "Two steps in flight", "The review, earned", "Stops", "Usage") still exists under that name, or the naming file is carried to the new name.

## Report

Write it to `.scratch/1-one-layout-for-every-skill/agents/reviews/4-report.md`: first line, anything NOT done or "Everything in the brief is done"; the open items verbatim; the DONE / NOT DONE table with the checks' output verbatim; files with line counts; judgment calls; the before and after of the skill's sections.
