# Brief: 7, refute restyled to the skill layout, with its rule inventory

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `docs/dev/skill-layout.md`, every ruling in `.scratch/1-one-layout-for-every-skill/plan.md`, and `skills/spec/SKILL.md` as landed in step 6 (stops and refusals told apart; a sequence as a numbered subsection of Steps).

## What is on the tree (read on main at e4950d0)

- `skills/refute/SKILL.md` is 44 lines, version 1.3.0, with the sections What it reads, What the reviewer looks for, What the reviewer runs, The runs over the repair rounds, What it writes, Rules.
- `python3 utils/check_skill_layout.py skills/refute` prints 9 error lines.
- No other file names a section of it (`grep -rn` for its section names over skills, docs and README.md finds only plan-orchestration's own sentence on the runs over the repair rounds); the refute skill's `templates/report.md` holds the report's headings, which stay as they are.

## What to build

1. `skills/refute/SKILL.md` rewritten to `docs/dev/skill-layout.md`, version 1.4.0.
2. `.scratch/1-one-layout-for-every-skill/inventories/refute.md`: one row per rule of the old file, `- Old:` at `e4950d0`.

## What it must do

- Every rule of the old file keeps its meaning, each written once, one rule per bullet, item or row.
- The four headings (spec, proof, standards, behaviour) and what each looks for, the runs over the repair rounds, what the reviewer may run, and the disposition of a finding keep their meaning exactly; the report's headings in `templates/report.md` stay valid.

## Verify before you report

From the worktree's root: `python3 utils/check_skill_layout.py skills/refute` prints `ok:`; `python3 utils/check_rule_inventory.py .scratch/1-one-layout-for-every-skill/inventories/refute.md` prints `ok:`; every command of the verify list passes.

## Report

Write it to `.scratch/1-one-layout-for-every-skill/agents/reviews/7-report.md`, in the end state, with every changed file's line count, the grep for removed section names over skills, utils, docs and README.md with its output, and the before and after of what a user of `/refute` reads differently.
