# Brief: 6, deep-research's files marked in the coverage list; the check over the whole list

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `docs/roadmap.md` entries 3 to 16 (what each new skill covers), `utils/check_coverage.py`'s docstring (the list's format), every ruling in `.scratch/2-coverage-inventory-of-the-academic-skills/plan.md`, and `docs/academic-coverage.md` as it stands (the introduction and the `academic-paper`, `academic-paper-reviewer` and `academic-pipeline` sections, whose rows the new section may point at).

## What is on the tree (read on main at 7752a76)

- `/Users/axelfaes/workspace/research-hub/.agents/skills/deep-research` holds 52 files (`find ... -type f | wc -l`): `SKILL.md`, 14 under `agents/`, 8 under `examples/`, 23 under `references/`, 6 under `templates/`; 11,775 lines (`find ... -type f | xargs cat | wc -l`). No link (`find ... -type l | wc -l` prints 0).
- `docs/academic-coverage.md` has the introduction, the New skills table and the `## academic-paper`, `## academic-paper-reviewer` and `## academic-pipeline` sections; its check command names those three skills.

## What to build

1. In `docs/academic-coverage.md`, the `## deep-research` section after the `academic-pipeline` section, with the same table (`| File | Mark | Reason |`).
2. Each of the 52 files read in full, and marked:
   - `rebuild: <skill>` when the new skill that entry builds must cover what the file does;
   - `rebuild later: <skill>` when it belongs to a new skill but is not needed for that skill's first gate;
   - `drop` when no new skill needs it.
   - The reason names what the file holds and why it goes where it goes, in one sentence or two, with what makes it worth keeping or dropping (for example a rule no other file states, a duplicate of another file, a feature outside Axel's work).
3. The introduction's check command names all four skills, and the introduction says the list covers every file of the four skills.

## What it must do

- No file is marked from its name: each reason states something only a reading of the file gives.
- A file whose content splits across skills is marked for the skill that takes most of it, and the reason names where the rest goes or says it is not carried.
- A reason that sends content to another skill's files names files that exist when that skill's roadmap entry is built: content a skill needs at its first gate is not sent to a skill whose entry comes later.
- A drop that says a rule is kept elsewhere names the file that keeps it, and that file states the rule.
- The list follows the prose standard (`skills/repo-setup/templates/docs/dev/prose-standard.md`): ASCII, one paragraph per source line, no history, no sentence longer than about 35 words, and no sentence shape that recurs across rows of the whole list (a reason opening with a label and a colon, one ending repeated from row to row).

## Verify before you report

From the worktree's root: `python3 utils/check_coverage.py docs/academic-coverage.md /Users/axelfaes/workspace/research-hub/.agents/skills academic-paper academic-paper-reviewer academic-pipeline deep-research` prints `ok: docs/academic-coverage.md`; every command of the verify list passes.

## Report

Write it to `.scratch/2-coverage-inventory-of-the-academic-skills/agents/reviews/6-report.md`, in the end state: the DONE table, the list's line count, the check's output, the count of each mark with the new skill it names, and the files whose marks were judgment calls with the reason.

## Decisions taken in this brief

- Sentence length is held to about 35 words: a reason lists what a file holds, which the prose standard's "unless the mechanism needs more" allows past 20 words, and 35 keeps a list readable. Reversible by editing this line and the rows.
