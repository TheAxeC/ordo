# Brief: 2, the coverage check's test joined to the checks

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `docs/dev/building.md`, the README's Tests section, and every ruling in `.scratch/2-coverage-inventory-of-the-academic-skills/plan.md`.

## What is on the tree (read on main at d44092c)

- `utils/check_coverage.test.sh` exists and prints `PASS: check_coverage.py scratch tests` on main.
- The seven tests are listed in `README.md` (the Tests block at lines 104-112 and one bullet each at lines 114-120), `docs/dev/building.md` (the command block, line 12 is the last test), `docs/dev/change-standard.md` (the "Commands and their filters" block, line 48 is the last test) and the state file's `verify` list (line 13 is the last test); `grep -n "check_coverage" README.md docs/dev/*.md` finds nothing.

## What to build

1. `README.md`: `sh utils/check_coverage.test.sh` in the Tests block after the rule inventory test, and a bullet after the rule inventory bullet stating what the test checks, in the same style.
2. `docs/dev/building.md`: the same command after the rule inventory test, with a comment in the block's style.
3. `docs/dev/change-standard.md`: `sh utils/check_coverage.test.sh 2>&1 | tail -1` after the rule inventory test.
4. The `verify` list: the same line in the same place, written on main at landing, since the ledger is written only on main.

## What it must do

- Every place that lists the tests lists this one, and no sentence counting the tests is left false: grep `seven`, `the tests`, `check_rule_inventory.test.sh` across `skills/`, `utils/`, `docs/` and `README.md`.

## Verify before you report

From the worktree's root: every command of `docs/dev/building.md` as changed passes, eight `PASS:` lines among them.

## Report

Write it to `.scratch/2-coverage-inventory-of-the-academic-skills/agents/reviews/2-report.md`, in the end state, with every changed file's line count, the verify lines, the grep with its output, and the before and after of each page.
