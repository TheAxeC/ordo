# Brief: 3, academic-paper's files marked in the coverage list

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `docs/roadmap.md` entries 3 to 16 (what each new skill covers), `utils/check_coverage.py`'s docstring (the list's format), and every ruling in `.scratch/2-coverage-inventory-of-the-academic-skills/plan.md`.

## What is on the tree (read on main at 64e50ce)

- `/Users/axelfaes/workspace/research-hub/.agents/skills/academic-paper` holds 61 files (`find ... -type f | wc -l`): `SKILL.md`, 12 under `agents/`, 10 under `examples/`, 27 under `references/`, 11 under `templates/`; about 16,000 lines (`wc -l`). No link.
- `docs/academic-coverage.md` does not exist.

## What to build

1. `docs/academic-coverage.md`: a title; an introduction that says what the list is for, where the files come from (research-hub's `.agents/skills/`, from `imbad0202/academic-research-skills`, with the `computedHash` of each skill in research-hub's `skills-lock.json`), what each mark means, and the command that checks it; the New skills table, one row per new skill of roadmap entries 3 to 15, by the skill's name (`writing`, `code-comments`, `paper`, `paper-review`, `rebuttal`, `grant`, `literature`, `idea`, `scaffold`, `project-docs`, `researcher`, `submit-manuscript`, `submit-grant`) with its entry number; and the `## academic-paper` section.
2. Each of the 61 files read in full, and marked in the section's table:
   - `rebuild: <skill>` when the new skill that entry builds must cover what the file does;
   - `rebuild later: <skill>` when it belongs to a new skill but is not needed for that skill's first gate;
   - `drop` when no new skill needs it.
   - The reason names what the file holds and why it goes where it goes, in one sentence or two, with what makes it worth keeping or dropping (for example a rule no other file states, a duplicate of another file, a feature outside Axel's work).

## What it must do

- No file is marked from its name: each reason states something only a reading of the file gives.
- A file whose content splits across skills is marked for the skill that takes most of it, and the reason names where the rest goes.
- The list follows the prose standard (`skills/repo-setup/templates/docs/dev/prose-standard.md`): ASCII, one paragraph per source line, no history.

## Verify before you report

From the worktree's root: `python3 utils/check_coverage.py docs/academic-coverage.md /Users/axelfaes/workspace/research-hub/.agents/skills academic-paper` prints `ok: docs/academic-coverage.md`; every command of the verify list passes.

## Report

Write it to `.scratch/2-coverage-inventory-of-the-academic-skills/agents/reviews/3-report.md`, in the end state: the DONE table, the list's line count, the check's output, the count of each mark with the new skill it names, and the files whose marks were judgment calls with the reason.
