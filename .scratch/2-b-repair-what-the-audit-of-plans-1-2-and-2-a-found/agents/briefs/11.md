# Brief: 11, every coverage row of academic-paper checked against its file

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `docs/dev/skill-layout.md` and `skills/repo-setup/templates/docs/dev/prose-standard.md` (the prose standard's named exception allows about 35 words in a coverage table's reason cell).

## What is on the tree (read on main at f3a77c1)

- `find /Users/axelfaes/workspace/research-hub/.agents/skills/academic-paper -type f | wc -l` prints 61. The `## academic-paper` section of `docs/academic-coverage.md` (lines 50-115) holds 61 rows (`awk 'NR>50 && NR<116 && /^\| `/' docs/academic-coverage.md | wc -l`).
- `python3 utils/check_coverage.py docs/academic-coverage.md /Users/axelfaes/workspace/research-hub/.agents/skills academic-paper academic-paper-reviewer academic-pipeline deep-research` prints `ok: docs/academic-coverage.md`.
- The doc's lines 1-30 define the marks: `rebuild: <skill>` (the entry's skill covers what the file does before that entry's gate), `rebuild later: <skill>` (the skill's first gate does not need it), `drop` (no new skill needs it; the reason says why and names the file that keeps any rule kept elsewhere); a file whose content splits is marked for the skill that takes most of it, and the reason names where the rest goes.
- The roadmap entries the marks name are in `docs/roadmap.md` (entry 5, paper, at line 40: its goal and its gate decide what `rebuild: paper` and `rebuild later: paper` mean).
- A review sampled 57 rows of the four skills and found defects in academic-paper rows, in `.scratch/reviews/2026-09-24-audit/4-coverage-and-roadmap.md` findings 6, 7, 8, 11 and 13 (the `vlm_figure_verification`, `formatter_agent`, `imrad_template`, `intake_agent`, `abstract_bilingual_agent`, `abstract_writing_guide`, `ethics_checklist` and `ethics_review_agent` rows).

## What to build

1. **Every file read, every row checked.** For each of the 61 files of `research-hub/.agents/skills/academic-paper`, read the file in full, then its row: does the mark follow the doc's definitions against the roadmap entry it names (its goal and gate), does the reason say what the file holds that decides the mark, and does it name where each part goes that the mark does not take. A part of a file that no row carries is a defect: a capability left with no skill and no stated reason for the drop.
2. **Each defective row fixed** in `docs/academic-coverage.md`, in place: the mark, the reason, or both. A `rebuild` row whose reason defers part of the file ("can come later") is defective: either the reason stops deferring it, or the part is named with the skill and entry that take it. The findings 6, 7, 8 and 13 of the review are checked this way and fixed where they hold; a finding that does not hold is recorded with the file's lines that show it.
3. **The intake row (ruling L (a)).** The `agents/intake_agent.md` row names style calibration (intake Step 10, learning the author's voice from three or more past papers) as taken by the paper skill at entry 5, as a guide subordinate to the prose standard; its reason no longer says the paper skill has no use for it.
4. **The records.** `.scratch/2-b-repair-what-the-audit-of-plans-1-2-and-2-a-found/agents/reviews/11-rows.md`: a table with one row per file, in the order of the coverage section: the file, the lines read (`1-<n>`, from `wc -l`), the verdict (`holds` or `fixed`), and for a fixed row what changed and why, with the file's lines that decide it. The record count equals 61.

## Cases

Turn these into checks first and run them against the unchanged list; report any the rules above get wrong before changing a row.

- `python3 utils/check_coverage.py ...` (the command above) passes before and after.
- The count of data rows in `11-rows.md` equals the count of files `find ... -type f` lists, 61, and each file name appears once.
- Every row of the section whose mark changed also appears as `fixed` in the records, and every `fixed` record's row differs from main's (`git` is not yours to run: compare with a copy of main's section taken before the first edit).

## Decisions taken in this brief (each reversible, none silent)

1. The records go in a file of their own beside the report, as the plan's step line names.
2. A reason that grows past about 35 words to name where a split part goes is shortened elsewhere, not left long.

## Read, with line ranges

1. `docs/academic-coverage.md` lines 1-115.
2. `docs/roadmap.md` whole (the entries the marks name).
3. `.scratch/reviews/2026-09-24-audit/4-coverage-and-roadmap.md` lines 1-21 and 22-118.
4. Every file of `/Users/axelfaes/workspace/research-hub/.agents/skills/academic-paper`, whole. Read only: nothing in research-hub is written.

## What it must do

- The coverage check above prints `ok: docs/academic-coverage.md` and exits 0.
- `11-rows.md` holds 61 records, one per file.
- The paths this step writes: `docs/academic-coverage.md` lines 50-115 (the academic-paper section) and, in the ledger, `agents/reviews/11-rows.md` and `agents/reviews/11-report.md`. Nothing else: steps 4 and 6a run beside this one on the plan-orchestration and plan-retro skills and on `README.md`.

## Conventions

- Plain prose in every reason, ASCII only, no history (no dates, no "the review found"), each cell one line.
- Never run a git command; work only in the worktree; research-hub is read only.

## Verify before you report

Run from the worktree root, each must hold:

1. `sh utils/verify.sh .scratch/2-b-repair-what-the-audit-of-plans-1-2-and-2-a-found/orchestrator-state.md` prints ten `PASS:` lines, ten `ok:` lines and `verify: 12 commands passed`, and exits 0.
2. The coverage check, with what it prints.
3. The record count: the command that counts the data rows of `11-rows.md`, and what it prints.

## Report

Write it to `.scratch/2-b-repair-what-the-audit-of-plans-1-2-and-2-a-found/agents/reviews/11-report.md` in the worktree's copy of the ledger, and the records to `11-rows.md` beside it; nothing else in the ledger is written. First line: anything NOT done, or "Everything in the brief is done". Then the open items of the state file, verbatim. Then the result table: each item of "What to build" and each check, DONE or NOT DONE, with the command's summary line. Then the rows changed, each with its before and after mark and a one-line why, and each of the review's findings 6, 7, 8 and 13 with its disposition. Then anything in the brief that was wrong, with the evidence. No pasted output beyond that.
