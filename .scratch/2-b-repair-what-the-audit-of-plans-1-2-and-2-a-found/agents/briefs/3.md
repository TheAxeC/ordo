# Brief: 3, skill texts part 2: spec, refute, land, plan-help, plan-retro, roadmap, ordo-init, repo-setup

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `docs/dev/skill-layout.md` and `skills/repo-setup/templates/docs/dev/prose-standard.md`.

## What is on the tree (read on main at 593ac19)

Line numbers were read with `cat -n` or `grep -n` on main; the findings are quoted from `.scratch/reviews/2026-09-24-audit/2-restyled-skills.md` (sections spec, refute, land, plan-help, ordo-init, roadmap, plan-retro, repo-setup) and `.scratch/reviews/2026-09-24-audit/6-oculus-changes.md` (F10, F11), which the builder reads in full.

- `skills/spec/SKILL.md`: Steps 2 (line 47) "A premise found false is a stop"; Stops row "A false premise" (line 95) and row "A scope change" (line 96); Steps 4 (line 57) commits "any amendment to `plan.md` the premise checks forced", which can only exist when a false premise is not always a stop. Stops row "A step in flight" (line 101) resumes on "That step landed" only.
- `skills/refute/SKILL.md`: line 10 reads "`/refute <entry> <step>` has one reviewer, who changes nothing, do what a builder's report cannot do for itself"; Steps 7 (line 53) records "the reviewer line in the dispatch block"; "Over a repair round" item 1 (line 58) and Rules (line 100) cap the runs at "at most `repair_rounds`", while `land` (Stops row "The step not ready", line 96) and `plan-orchestration` allow one round more under the exception. The restyle split "No background shells, no polling, no benchmark suites, no sanitizer runs unless the brief lists them" so that the qualifier "unless the brief lists them" no longer covers background shells and polling (review, refute finding 2).
- `skills/land/SKILL.md` (114 lines, read whole): the look is the last bullet of Steps 5 (line 51) and "The landing script" (line 86) says `land.sh`'s zero exit is step 5's pass; Steps 7 books "the usage row" that Steps 8 produces; Steps 11 (the landing report) is written before Steps 9's commit; after a red line that takes the step back out of main, nothing says what becomes of the dispatch block (left at `landing: cherry-picking`), the worktree or the branch; no step stops the step's builder and reviewers before the wip commit of Steps 2; Rules (lines 112-113) "undoing a step is `git revert` of that commit ... A reverted step is booked as reverted"; Steps 11 (line 70) opens the landing report with the open items and no position line.
- `skills/plan-help/SKILL.md`: the printed sequence says "/land refuses or stops ... fix it at landing or book it, then /land again" (line 68), which is wrong for a red line that takes the step out of main; it says "booked items" where every other skill says "booked list"; line 70 says `/plan-orchestration` runs "an agent at "build it"", while the executor can be `inline`.
- `skills/plan-retro/SKILL.md`: its proposal order ends with "a sharper sentence for the existing rule" (review, plan-retro finding 1), which `plan-orchestration`'s Anti-patterns row "Rewriting a rule that keeps being broken" forbids.
- `skills/roadmap/SKILL.md`: Steps 2-5 (draft, show a diff, write, commit) read as applying to every command, the plain `/roadmap` (subsection "Show") included; Stops row "The change" (line 108) stops on every change.
- `skills/ordo-init/SKILL.md`: Rules (line 105) "The skill writes nothing until the user approves or corrects the draft", while Steps 3 (line 49) runs each verification command once before approval; Rules (line 109) "Every path is relative to the repository root", while `launch_note` must be absolute; the old "It never overwrites an existing page or `.agents/plan.yaml`" survives only as the Anti-patterns row at line 101, without "never".
- `skills/repo-setup/SKILL.md`: question 5 (line 86) asks for the repository's commit rule, and the sync's Steps 8 (line 78) honours it with the Stops row "No commit allowed" (line 121); the setup's Steps 13 (line 60) commits whatever the answer.
- The rule inventories of plan 1, `.scratch/archive/1-one-layout-for-every-skill/inventories/{spec,refute,land,plan-help,plan-retro,roadmap,ordo-init,repo-setup}.md`; `python3 utils/check_rule_inventory.py .scratch/archive/1-one-layout-for-every-skill/inventories/*.md` prints ten `ok:` lines on main.

## What to build

`spec`:
1. A false premise stops only when the plan cannot absorb it (ruling 3c): a premise the plan can absorb is corrected in `plan.md` in the preparation commit, and the brief records the correction; a correction that changes the step's scope, or a choice the user would see, is a stop. Steps 2 and the Stops rows say this once and agree with Steps 4.
2. The Stops row "A step in flight" resumes when that step landed, or when its landing was taken back out of main and its dispatch block marked so (item 6).

`refute`:
3. Line 10 reads as a sentence: `/refute` dispatches one reviewer, who changes nothing and does what a builder's report cannot do for itself.
4. Steps 7 records the review under the dispatch block's `reviewer_report` field.
5. The runs over the repair rounds are "at most `repair_rounds`, or one more under `plan-orchestration`'s exception", in "Over a repair round" and in Rules.
6. The qualifier "unless the brief lists them" covers background shells and polling again, as it covers benchmark suites and sanitizer runs.

`land`:
7. A new first step, before the wip commit: stop the step's builder and every reviewer through the runner's stop tool, check the runner's agent listing shows none of them left, and for a shell builder check that its pid is gone and its exit file exists; the landing report names that check. The words name no vendor and no tool name.
8. The look is its own numbered step, after the checks on main and before the A/B, and "The landing script" says `land.sh`'s zero exit is the checks' pass, not the look's.
9. The steps are numbered in execution order: the usage row, then the booking, the state file, the landing report, the commit, the worktree removal.
10. A red line that takes the step back out of main sets the dispatch block's `landing: backed-out`, keeps the worktree and the branch, leaves the step unticked, and books the failure as Steps 5 says.
11. Rules: a landed step found short of its brief, or wrong, gets a new step that finishes it on top of what landed; a landed commit is reverted only on the user's ruling (ruling B). One step stays one implementation commit, which keeps each step traceable. "A reverted step is booked as reverted" holds only for a revert the user ruled.
12. The landing report opens with the position line (the roadmap entry with its title, the plan step as "step n of m" with its name, the next step), then the open items verbatim, as `plan-orchestration`'s Reports section will say.

`plan-help`:
13. The printed sequence splits the `/land` line into two: a refusal (fix what it names, then `/land` again) and a red line (the step is taken back out of main and booked; the booked step comes next). It says "booked list", and it says `/plan-orchestration` builds each step with the executor the plan names (an agent by default).

`plan-retro`:
14. Ruling 3b: the proposal for a rule already written that keeps being broken is a check; a sharper sentence is proposed only when no command can check the rule, and the proposal says why no command can.

`roadmap`:
15. Steps 2-5 apply to `add`, `move`, `done` and `drop` only; the plain `/roadmap` (Show) changes nothing and shows nothing for approval.

`ordo-init`:
16. Rules: every path is relative to the repository root, except `launch_note`, which is an absolute path; the skill writes nothing until the user approves, except that each verification command runs once from its directory before the draft is shown (its output is shown with it); and it never overwrites an existing page or `.agents/plan.yaml`: a change to one is shown as a diff and made after approval.

`repo-setup`:
17. The setup's Steps 13 commits only when the answer to question 5 allows it; otherwise it stops with the Stops row "No commit allowed", as the sync's Steps 8 does.

The inventories:
18. Every row of the eight inventories whose new place moved names the place that now holds its rule, checked by reading the text at that place.

## Decisions taken in this brief (each reversible, none silent)

1. The backed-out state is written `landing: backed-out`; it is a value of the dispatch block, which only the ledger reads. The resumption case for it in `plan-orchestration` is not this step's (another step holds that file); it is booked for the step that next edits `plan-orchestration`.
2. The skill texts that still say to pipe each check through the rules file's filter (`refute` line 44, `spec`'s `templates/brief.md` line 32, `land` Steps 5) are left as they are in this step; where the verify runner lives is an open item for the user, and the text that names it follows the ruling.

## Read, with line ranges

1. The eight `SKILL.md` files named above, whole.
2. `.scratch/reviews/2026-09-24-audit/2-restyled-skills.md`, the sections of these eight skills and "Contradictions between skills, collected".
3. `.scratch/reviews/2026-09-24-audit/6-oculus-changes.md`, findings F10 and F11.
4. `.scratch/2-b-repair-what-the-audit-of-plans-1-2-and-2-a-found/plan.md`, the Rulings sections (3b, 3c, B).
5. The eight inventories named above, whole, and `utils/check_rule_inventory.py`'s docstring.

## What it must do

- Every item above is in the text, each rule written once where `docs/dev/skill-layout.md` ("Where a rule goes") puts it.
- `python3 utils/check_skill_layout.py` prints ten `ok:` lines and exits 0.
- `python3 utils/check_rule_inventory.py .scratch/archive/1-one-layout-for-every-skill/inventories/*.md` prints ten `ok:` lines, and every moved row names the place that holds its rule.
- The paths this step writes: `skills/{spec,refute,land,plan-help,plan-retro,roadmap,ordo-init,repo-setup}/SKILL.md` and their eight inventories. Nothing else: other steps run beside this one and hold `skills/plan-orchestration`, `skills/plan`, the templates, `utils/` and the pages.

## Conventions

- The prose standard in every new or changed sentence: ASCII, no history, no repeated construction, sentences of about 20 words unless the mechanism needs more, plain and active where the actor matters.
- One rule per bullet; a rule written once, and another place that needs it names its section.
- The skill names no vendor and no tool name ("the runner's stop tool", "the runner's agent listing").
- Markdown paragraphs and bullets are one source line each.

## Verify before you report

Run from the worktree root, each must hold:

1. `sh utils/verify.sh .scratch/2-b-repair-what-the-audit-of-plans-1-2-and-2-a-found/orchestrator-state.md` prints ten `PASS:` lines, ten `ok:` lines and `verify: 12 commands passed`, and exits 0.
2. `python3 utils/check_rule_inventory.py .scratch/archive/1-one-layout-for-every-skill/inventories/*.md` prints ten `ok:` lines.
3. For each numbered item of "What to build", a `grep -n` that shows its text in place, quoted in the report.
4. For each moved inventory row, the row and the text at its new place, quoted side by side.

## Report

Write it to `.scratch/2-b-repair-what-the-audit-of-plans-1-2-and-2-a-found/agents/reviews/3-report.md` in the worktree's copy of the ledger; nothing else in the ledger is written except the eight inventories. First line: anything NOT done, or "Everything in the brief is done". Then the open items of the state file, verbatim. Then the DONE / NOT DONE table, one row per item with its check and output verbatim. Then files with line counts, every judgment call the brief left open, every user-visible change with its before and after, and anything in the brief that was wrong or impossible, with the evidence.
