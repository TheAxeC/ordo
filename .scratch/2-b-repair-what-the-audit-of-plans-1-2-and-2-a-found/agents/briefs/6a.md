# Brief: 6a, collect_findings.py keeps every item under the four headings as a finding

Read `docs/dev/change-standard.md` first; its rules govern this step unchanged. Then, in full: `docs/dev/skill-layout.md` and `skills/repo-setup/templates/docs/dev/prose-standard.md`.

## What is on the tree (read on main at 129a3f7)

`wc -l`: `skills/plan-retro/templates/collect_findings.py` 293, `collect_findings.test.sh` 459, `skills/plan-retro/SKILL.md` 110, `skills/plan-retro/templates/retro.md` 40.

- `collect_findings.py:54` defines `CLOSURE`, `:55` `FIRST_SENTENCE`, `:59-61` `NOTHING_FOUND`, `:62-64` `OTHERS_REPRODUCE`, with the comment at `:56-58` (`grep -n 'NOTHING_FOUND\|OTHERS_REPRODUCE\|CLOSURE' collect_findings.py`).
- `reports_nothing` (`:185-188`) drops an item whose first sentence opens with a `NOTHING_FOUND` form or says only that the other figures reproduce; `findings` calls it at `:198`, and drops a round's item that `CLOSURE` matches at `:202`.
- The docstring's paragraph at `:18-21` states both drops.
- `collect_findings.test.sh` head comment (`:4-19`) and its fixtures assert the drops: the "none" forms (for example `:75`, `:88`, `:130`, `:175`, `:210`), the closure bullets of a round (`:100`, `:111`, `:198`), and the list of no-finding forms beside near misses at `:249-262`.
- `skills/plan-retro/SKILL.md` Grouping (`:74`) already sets aside, by reading, a finding whose text reports no defect, as the kind "no defect", listed in the retro's "No defect" section (`templates/retro.md:36-40`). It says nothing of a closure that holds.
- `README.md:119`, the bullet of `collect_findings.test.sh`, describes the dropped forms ("a closure that holds, or an item in one of the forms that report nothing", "each near miss of those forms").

## What to build

1. **The collector keeps every item.** In `collect_findings.py`, every top-level item under a Spec, Proof, Standards or Behaviour heading, and every item of a "Repair round <n>, refuted" section outside its unread subsections, is a finding. Remove `NOTHING_FOUND`, `OTHERS_REPRODUCE`, `CLOSURE`, `reports_nothing`, the comment above them, and `FIRST_SENTENCE` when nothing else uses it. The sections and subsections named Verification, Not checked, Closed, Closures and Usage stay unread (`NOT_READ`), and fences stay skipped: those are sections and fences, not word lists. The docstring says every item under the headings is a finding, and that the retro sets aside by reading what reports no defect or a closure that holds.
2. **The tests.** Remove the fixtures and assertions that exist only to check a dropped form, and the head comment's lines that name them. Keep one assertion that an item reading "None." under a Spec heading, one reading "The other figures reproduce." and a round's item reading "Spec 1: closed." each come out as findings, so the test turns red when any of the removed filters is put back. Every other case of the test stays and passes.
3. **The retro skill sets aside by reading.** `skills/plan-retro/SKILL.md` Grouping says the collector keeps every item, and that a finding whose text reports no defect or a closure that holds is set aside, by reading, as the kind "no defect", counted and listed in the "No defect" section with no proposal. Steps 1's first bullet and `templates/retro.md` agree with it; change `retro.md` only where it disagrees.
4. **The README bullet.** `README.md:119` becomes one sentence saying what `collect_findings.test.sh` covers.

## Cases

Turn these into the step's test assertions first and run them against the unchanged collector; report any case the rules above get wrong before changing code.

- Under `## 1. Spec`, the item `- None.` is one finding with heading `spec`.
- Under `## 2. Proof`, the item `- The other figures reproduce.` is one finding with heading `proof`.
- In `## Repair round 1, refuted` with no subheadings, the item `- Spec 1: closed.` is one finding with heading `unclassified`.
- Under `## Closed`, `## Not checked`, `## Verification` and `## Usage`, no item is a finding.
- Inside a fence under `## 1. Spec`, no line is a finding.

## Decisions taken in this brief (each reversible, none silent)

1. The unread sections (`NOT_READ`) stay: they are not the word lists the cut removes, and reading "Closed" would count the orchestrator's dispositions as findings.
2. A round's closure item with no subheading and no trailing heading word comes out `unclassified`, as any other such item does.

## Read, with line ranges

1. `skills/plan-retro/templates/collect_findings.py` and its test, whole.
2. `skills/plan-retro/SKILL.md` whole and `skills/plan-retro/templates/retro.md` whole.
3. `README.md` lines 100-130.

## What it must do

- `sh skills/plan-retro/templates/collect_findings.test.sh 2>&1 | tail -1` prints `PASS: collect_findings.py scratch tests`.
- `grep -n 'NOTHING_FOUND\|OTHERS_REPRODUCE\|CLOSURE\|reports_nothing' skills/plan-retro/templates/collect_findings.py` prints nothing.
- The paths this step writes: `skills/plan-retro/templates/collect_findings.py`, `skills/plan-retro/templates/collect_findings.test.sh`, `skills/plan-retro/SKILL.md`, `skills/plan-retro/templates/retro.md`, and `README.md` line 119. Nothing else: step 4 runs beside this one on `launch.sh`, the plan-orchestration skill, `skills/plan/templates/orchestrator-state.md` and `README.md` line 121.

## Conventions

- Python in the style of the file, POSIX `sh` in the test, ASCII only, no history in comments or docstrings, lines of about 100 characters at most.
- The skill layout on `skills/plan-retro/SKILL.md`; `python3 utils/check_skill_layout.py` prints `ok:` for it.
- Never run a git command; work only in the worktree.

## Verify before you report

Run from the worktree root, each must hold:

1. `sh utils/verify.sh .scratch/2-b-repair-what-the-audit-of-plans-1-2-and-2-a-found/orchestrator-state.md` prints ten `PASS:` lines, ten `ok:` lines and `verify: 12 commands passed`, and exits 0.
2. The two commands of "What it must do", with what they print.
3. Each removed filter put back on a copy under `$TMPDIR` (the `NOTHING_FOUND` drop, the `OTHERS_REPRODUCE` drop, the `CLOSURE` drop), each turning the copied test red, with its first `FAIL:` line.

## Report

Write it to `.scratch/2-b-repair-what-the-audit-of-plans-1-2-and-2-a-found/agents/reviews/6a-report.md` in the worktree's copy of the ledger; nothing else in the ledger is written. First line: anything NOT done, or "Everything in the brief is done". Then the open items of the state file, verbatim. Then the result table: each item of "What to build" and each check, DONE or NOT DONE, with the command's summary line. Then the planted faults, each with its red line. Then files with line counts, every user-visible change with its before and after, and anything in the brief that was wrong, with the evidence. No pasted output beyond that.
