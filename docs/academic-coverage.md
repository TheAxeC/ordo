# Coverage of the academic skills

For each academic skill installed in research-hub that has a section below, this list says what happens to each of its files when Ordo's own writing skills replace the skill (roadmap entries 3 to 16). Each file is marked with the new skill that must cover what it does, the new skill that will cover it later, or `drop`, and the reason says what the file holds that decides it. A new skill's plan starts from the rows that name it.

The files are those in `research-hub/.agents/skills/` of the skills `academic-paper`, `academic-paper-reviewer`, `academic-pipeline` and `deep-research`, installed from `imbad0202/academic-research-skills`. Their `computedHash` values in research-hub's `skills-lock.json` when this list was written:

- `academic-paper`: `54cd6f3549239f10821d688f0b2894c165b8ee3162fb235711f1223e08973f8b`
- `academic-paper-reviewer`: `6940aa353c2ad3ff52082a33487e4a98fd6813aed6d25929f4f82a499c21b9fd`
- `academic-pipeline`: `04dea9ef49eae79fd2f4ce6a04765f15ee7afe32b79a9bdcb72cf7cfc54fea38`
- `deep-research`: `e20235bd776f36c9e8cceb38fe71713ad008695d92e91f7869a3c6c8cba964ad`

The marks:

- `rebuild: <skill>`: the new skill that roadmap entry builds must cover what the file does before that entry's gate.
- `rebuild later: <skill>`: the file belongs to that new skill, but the skill's first gate does not need it.
- `drop`: no new skill needs the file; the reason says why, and where a rule in it is kept elsewhere, it names the file that keeps it.

Every file was read in full before it was marked. A file whose content splits across skills is marked for the skill that takes most of it, and the reason names where the rest goes.

The check, run from the repository root over the skills that have a section below, exits 0 only when every file of the named skills is listed exactly once with a valid mark and a reason:

```sh
python3 utils/check_coverage.py docs/academic-coverage.md /Users/axelfaes/workspace/research-hub/.agents/skills academic-paper academic-paper-reviewer academic-pipeline
```

## New skills

| Skill | Roadmap entry |
|---|---|
| writing | 3 |
| code-comments | 4 |
| paper | 5 |
| paper-review | 6 |
| rebuttal | 7 |
| grant | 8 |
| literature | 9 |
| idea | 10 |
| scaffold | 11 |
| project-docs | 12 |
| researcher | 13 |
| submit-manuscript | 14 |
| submit-grant | 15 |

## academic-paper

| File | Mark | Reason |
|---|---|---|
| `SKILL.md` | rebuild: paper | Of eleven modes, the full one runs eight phases in which the outline is approved before the argument, revisions stop after two rounds, and every claim is cited with no orphan citations. The paper skill takes these, `rebuttal` the revision-coach and rebuttal-audit modes; the Chinese triggers, higher-education defaults and generator-evaluator machinery stay out. |
| `agents/abstract_bilingual_agent.md` | rebuild later: paper | Under the zh-TW material the file states language-neutral abstract rules: five parts, 150 to 300 words, five to seven keywords that do not repeat the title, no citations, and every abbreviation defined. The zh-TW abstract itself is not carried, and the paper skill's first gate, a revision round, needs neither. |
| `agents/argument_builder_agent.md` | rebuild: paper | A thesis is built from three to five claim-evidence-reasoning chains, each paired with its strongest counter-argument and a rebuttal. Drafting stops when two weak-argument signs appear, such as circular reasoning, correlation taken as causation or an undefined key term. |
| `agents/citation_compliance_agent.md` | rebuild: paper | Citations and reference entries are cross-checked both ways for orphans and mismatches, along with DOI form, self-citation, source age and retractions; format is fixed automatically, while a claim the source lacks stops the check. The paper skill's `\cite`-against-`.bib` and DOI checks come from here; Chinese citation rules have no place in them. |
| `agents/draft_writer_agent.md` | rebuild: paper | The draft writer works section by section against the outline, builds topic-evidence-explanation-link paragraphs, requires a cited source to predate the event it evidences, and revises through hash-checked block patches. The paper skill does that patching with `tools/manuscript`'s `anchorize_tex.py` and `apply_tex_patch.py`, and has no place for the generator-evaluator phases, claim intent manifest or HTML-comment citation layers. |
| `agents/formatter_agent.md` | rebuild later: paper | The formatter converts a paper to LaTeX with a `.bib` or to DOCX without changing content (word count within 1 percent) and keeps journal-specific formatting profiles. The papers start from the venue's LaTeX template, so the first gate does not need it; the cover letter goes to `submit-manuscript`, and no new skill takes the provenance gates or zh-TW fonts. |
| `agents/intake_agent.md` | rebuild: paper | The configuration interview that fixes paper type, venue, citation format, word count, materials, co-authors and funding before writing, with venue limits recorded only as the scholar states them. The paper skill keeps those limits in the project until roadmap entry 13's `venues/` files hold them; it drops the zh-TW options, style calibration and evidence-profile machinery. |
| `agents/literature_strategist_agent.md` | rebuild: literature | Searches combine Boolean strings, citation chaining and semantic search under criteria fixed beforehand, with title-then-full-text screening, a source-by-theme matrix, five kinds of research gap and saturation stopping rules. An advisory fires when one period, place, method or venue holds 70 percent of known entries, and no new skill needs the Taiwan databases, passport corpus protocol or trust-chain fields. |
| `agents/peer_reviewer_agent.md` | rebuild: paper-review | One reviewer scores five weighted dimensions into a verdict, lists per-section issues with severity and fix, and checks that title, abstract and conclusion match the findings. Its re-review looks only at flagged items and what the revision broke, for at most two rounds, while its generator-evaluator phases are discarded. |
| `agents/revision_coach_agent.md` | rebuild: rebuttal | The revision coach parses reviewer comments into items with none dropped, splitting compound comments, classifying each, mapping it to a section, and extracting each promise with the evidence that would fulfil it. The user sees the parse before any response is written, and this becomes the rebuttal skill's point table. |
| `agents/socratic_mentor_agent.md` | rebuild later: paper | Planning a paper runs as a dialogue with a readiness check, thesis crystallisation, five questions per chapter, a four-type question taxonomy, and convergence signals with a cap on rounds. That comes after the paper skill's first gate, a revision round; its taxonomy also serves the `idea` interview, and nothing uses the commitment gates or wording-pattern list. |
| `agents/structure_architect_agent.md` | rebuild: paper | A structure is chosen from the paper type, and its outline sets each section's purpose and word count within 5 percent of the total, maps every source and adds transitions. The user approves the outline before the argument is built. |
| `agents/visualization_agent.md` | rebuild: paper | Chart types come from a decision tree, drawn at 300 dpi with a colourblind-safe palette and LaTeX figure templates, avoiding dual y-axes, truncated axes and missing error bars. Among ten checks per figure is that plotted values match the data, and a separate step traces each figure to its data and script, as roadmap entry 5's figures require. |
| `examples/chinese_paper_example.md` | drop | This complete zh-TW IMRaD paper on Taiwanese private-university enrolment uses fictional data and references to show the Chinese APA format. Neither the language nor the field is used, and the new skills draw their examples from real papers in the hub. |
| `examples/clinical_citation_verification_checklist.md` | rebuild: paper | Under a clinical example, the checklist asks whether the reference resolves, whether the source holds the exact number at a page, table or quote, whether population and metric match, and whether limitations survive. It is the manual form of the paper skill's citation check, minus the clinical safety note. |
| `examples/clinical_epistemic_status_example.md` | rebuild: paper | A draft may not state more than its source, and association turned into cause, a dropped hedge, a wider population or a stronger verb are each flagged with a rewrite. The paper skill's claim checks take this rule, restated without the clinical examples. |
| `examples/commitment_ledger_example.md` | rebuild: rebuttal | Each reviewer promise on an ML paper (a CIFAR-100 ablation, error bars over five seeds) tracked to fulfilled, partial or not fulfilled. A promise not kept needs one of three rationales, and a missing one is flagged; this is the rebuttal skill's trace from point to change. |
| `examples/imrad_hei_example.md` | drop | This complete IMRaD paper on AI-assisted accreditation in Taiwanese higher education carries illustrative references and a zh-TW abstract. `references/paper_structure_patterns.md` states the section shapes it shows, and the paper skill's worked example will be an ML paper from the hub. |
| `examples/literature_review_example.md` | drop | The example is a complete thematic literature review on enrolment decline in East Asia with illustrative references. `templates/literature_review_template.md` and the structure patterns state its structure of themes, cross-cutting synthesis, gaps and agenda. |
| `examples/plan_mode_guided_writing.md` | drop | A full plan-mode dialogue on a blended-learning paper, from the readiness check through the chapter plan. It applies the rules of `agents/socratic_mentor_agent.md` and states none of its own. |
| `examples/revision_mode_example.md` | rebuild: rebuttal | In this worked revision round the comments are parsed into major and minor items with their sections, the item that changes other sections goes first, and a before-and-after table carries page pointers. Its response letter, answering each comment with where it changed, is the shape of the rebuttal skill's output. |
| `examples/revision_recovery_example.md` | rebuild: rebuttal | A round in which one reviewer point is declined: it needs a principled reason, a line in the limitations and an explanation in the response. Each response follows comment, response, changes made, which the re-review checks item by item; these are the rebuttal skill's rules for declined points. |
| `examples/version_family_reconciliation_example.md` | rebuild later: literature | A worked case of one work in two versions (arXiv v1 and the NeurIPS proceedings): a sentence that quotes one version must cite that version, and the three acceptable fixes. It concerns which `.bib` entry a claim cites, common in ML where preprints and proceedings coexist, but is not needed for the literature skill's first gate. |
| `references/abstract_writing_guide.md` | rebuild later: paper | The guide sorts abstracts into structured, unstructured and extended conference types, gives word counts per venue kind, patterns for the five components, and keyword rules. It repeats the checklist of `agents/abstract_bilingual_agent.md`, so the two become one page, and its zh-TW half is discarded. |
| `references/academic_writing_style.md` | rebuild: writing | Every writing skill shares these prose rules, among them terms defined on first use, clear antecedents, a hedging scale with when not to hedge, and the tense per section. Only the engineering and computer-science register is kept, without the zh-TW conventions. |
| `references/anti_leakage_protocol.md` | rebuild: paper | Every claim, citation, number and method step in a draft comes from the project's own materials, and the methods describe only what the project documents. A topic the materials miss is flagged as a gap, filled from the model only when the user allows it and then tagged; for code projects the methods follow the code and logs. |
| `references/apa7_chinese_citation_guide.md` | drop | The guide gives Chinese-language APA 7 reference forms, mixed Chinese-English ordering and the conventions of Taiwanese TSSCI education journals. No paper in the hub cites in Chinese. |
| `references/apa7_extended_guide.md` | rebuild: paper | The statistics reporting rules no other file of the skill states: exact p values unless below .001, an effect size always reported, the 95 percent CI format and decimal places. The paper skill's statistics page takes them; the APA page rules (headings, title page, reference forms, bias-free language) are not carried, since the venue template sets the style. |
| `references/changelog.md` | drop | This log lists versions 1.0 to 2.5 of the paper skill and what changed in each, without any rule a new skill needs. |
| `references/citation_format_switcher.md` | drop | It converts citations by hand between APA, Chicago, MLA, IEEE and Vancouver, in-text and in the reference list. In the hub's LaTeX papers the venue's bibliography style renders every entry, so nothing is converted by hand, zh-TW notes included. |
| `references/credit_authorship_guide.md` | rebuild: paper | The fourteen CRediT roles with a lead-or-supporting matrix, the four ICMJE authorship conditions, what goes to the acknowledgments, and the rule that an AI tool is never an author. Disclosure statements are part of roadmap entry 5's goal, so the paper skill takes these rules without the higher-education examples. |
| `references/disclosure_mode_protocol.md` | rebuild: paper | Each category of AI use is marked used, not used or uncertain, and the user confirms the uncertain ones. The venue's phrasing and placement apply with the policy's access date, an unknown venue's policy is asked for rather than guessed, and the policy-anchor rendering stays behind. |
| `references/domain_evidence_profiles.md` | rebuild: literature | Of the per-field evidence profiles, the `cs_ml` one applies: archival preprints and conference proceedings count beside journals, and the risks to surface are non-reproducible results and benchmark cherry-picking. The literature skill adopts it as the default for ML work and ignores the profile machinery and other profiles. |
| `references/failure_paths.md` | rebuild later: paper | Twelve failure paths, three with rules stated nowhere else: desk rejections by cause, the conference-to-journal extension (30 to 50 percent new content), and a draft far off length handled by listing sections, never by deleting. None is needed for the paper skill's first gate, and the venue switch after a scope rejection will use the venue entries. |
| `references/funding_statement_guide.md` | rebuild: paper | Funding statements come in forms for one, several, no or partial funding with the funder's role, a funder's disclaimer is quoted verbatim, and a conflict-of-interest statement is always present. These statements belong to the paper skill's goal in roadmap entry 5; the Taiwanese agency formats are not carried, and each funder's acknowledgement text goes to the grant skill's config. |
| `references/hei_domain_glossary.md` | drop | This English and romanised-Chinese glossary covers Taiwanese higher-education terms such as institution types, accreditation, governance and HEEACT domains. The hub works in neither that field nor that language. |
| `references/journal_submission_guide.md` | rebuild: submit-manuscript | Before submission a checklist covers scope, the predatory-journal check, anonymisation and line numbers, then the required components and a cover letter with suggested and excluded reviewers. The response-letter part belongs to `rebuttal` and the data-availability templates to the paper skill's statements, while the higher-education journal tables serve no one. |
| `references/latex_template_reference.md` | rebuild later: paper | For LaTeX output the file gives an `article` template, the `apa7` class with its justification override and `\tabcolsep` formula, BibTeX entry forms, natbib and biblatex commands, compile fixes and Pandoc commands. Because each paper begins in its venue's template, conversion can wait; the BibTeX forms go with the literature skill's `.bib` writing, and nothing of the zh-TW settings is kept. |
| `references/mode_selection_guide.md` | rebuild: paper | Which mode fits what the user has, and the gate for turning a plan into a full draft (each chapter's argument adequate, the question fixed, five seed references). The paper skill's Quick start and Use instead take the routing, pointing to `rebuttal` and `literature`; the zh-TW trigger rows stay behind. |
| `references/paper_structure_patterns.md` | rebuild: paper | Six paper structures (IMRaD, thematic review, theoretical, case study, policy brief, conference paper) come with section skeletons, word allocations and a selection table. The paper skill needs the conference and IMRaD skeletons, which for ML papers become introduction, related work, method, experiments, results, discussion and conclusion, and none of the others. |
| `references/plan_mode_protocol.md` | rebuild later: paper | The plan-mode activation signals and the default to plan mode when a request is unclear; the flow itself restates `agents/socratic_mentor_agent.md`, and most signals are also in `SKILL.md` and `references/mode_selection_guide.md`. One signal is stated only here (results in hand, no idea how to shape them into a paper), so the file goes with the planning dialogue, which the first gate does not need. |
| `references/policy_anchor_disclosure_protocol.md` | rebuild: paper | The file decides when a no-AI statement is written and when nothing is, and requires AI-quoted text to be attributed but never cited as a primary source. The paper skill's disclosure page takes these, its venue entries take the copyediting carve-out that differs by venue, and no skill needs the four-anchor rendering. |
| `references/policy_anchor_table.md` | drop | The verbatim sixteen-field policy matrix for PRISMA-trAIce, ICMJE, Nature and IEEE, with snapshot hashes, behind the policy-anchor rendering, which is not carried. The paper skill's venue entries for Nature and IEEE, including the copyediting carve-out and IEEE's acknowledgments placement, are written from each policy's own page with its access date. |
| `references/revision_patch_protocol.md` | rebuild: paper | A revision round runs as anchorize, patch, apply, in which block ids are never renumbered, one stale hash rejects the whole patch with the base untouched, and a structural change is refused unless acknowledged. Patches are never hand-edited, and `tools/manuscript`'s `anchorize_tex.py` and `apply_tex_patch.py`, which roadmap entry 5 moves into the paper skill, already do this. |
| `references/statistical_visualization_standards.md` | rebuild: paper | Behind the figure rules sit when not to draw a figure, subtle errors such as unequal histogram bins, palettes with hex codes, matplotlib and ggplot2 templates, and LaTeX figure environments. The paper skill merges it with `agents/visualization_agent.md` into one figures page. |
| `references/venue_disclosure_policies.md` | rebuild: paper | The AI-use policy of ICLR, NeurIPS, Nature, Science, ACL and EMNLP with source URL, access date, required phrasing and placement, and the rule that an unlisted venue's policy is pasted by the user, never guessed. The disclosure step needs it at roadmap entry 5, so the paper skill holds these entries until entry 13 moves them into the researcher's `venues/` files. |
| `references/vlm_figure_verification.md` | rebuild: paper | A vision model reads each rendered figure against its data, and a trace links each figure to its data and script with every citing claim checked. The trace belongs to the paper skill's figures page; the vision check can come later. |
| `references/workflow_phase_details.md` | drop | A per-phase summary of the eight phases, their agents, outputs and checkpoints. Every item is stated in `SKILL.md` and the agent files. |
| `references/writing_judgment_framework.md` | rebuild: writing | Prose judgment rests on the clarity test of removing a paragraph, four questions a reader must be able to answer, the voice each discipline trusts, and the "so what" of each section. The revision decision matrix at the end goes to `rebuttal`. |
| `references/writing_quality_check.md` | rebuild: writing | The writing base checks an anti-pattern list, among them filler terms, more than three dashes per paper, throat-clearing openers, forced groups of three, synonym cycling, binary contrasts and five equal-length sentences in a row. Roadmap entry 3 names the dash and anti-pattern checks. |
| `templates/bilingual_abstract_template.md` | drop | A fill-in template pairing an English and a zh-TW abstract with an independence checklist. The English half repeats the five parts of `references/abstract_writing_guide.md`; the zh-TW half is not used. |
| `templates/case_study_template.md` | drop | The template lays out a case-study paper with case selection, a timeline, themes, transferability and lessons for practitioners. No paper in the hub is a case study. |
| `templates/conference_paper_template.md` | rebuild: paper | Conference papers get a skeleton of motivation, question, a contribution paragraph stating specifically what is new, related work ending in the gap, method, results per finding, limitations and conclusion. Most papers in the hub are conference papers, so this is the paper skill's default skeleton, adapted to the venue template. |
| `templates/credit_statement_template.md` | rebuild: paper | The CRediT form asks for an author table with ORCID and the corresponding author, the lead-or-supporting matrix, the statement, an equal-contribution sentence and a checklist, noting that all co-authors agree the roles. Together with `references/credit_authorship_guide.md` it forms one statements page, without the zh-TW form. |
| `templates/funding_statement_template.md` | rebuild: paper | Funding sources are registered with the funder's role in design and publication, and the form gives the statement per case and funder formats including ERC and Horizon Europe. The statements page joins it to `references/funding_statement_guide.md` and leaves out the Taiwanese formats. |
| `templates/imrad_template.md` | rebuild later: paper | The IMRaD journal skeleton with its subsections (context, problem, gap, questions, significance; framework and themes; design, sample, data, measures, analysis, validity, ethics; results per question; discussion with limitations and future work). The conference skeleton comes first; this one serves a journal version of a paper. |
| `templates/latex_article_template.tex` | drop | This generic APA-style `article` template sets double spacing, Times, natbib `apalike` and a commented zh-TW abstract. Each hub paper starts from its venue's own class and style file, which fix spacing, fonts and bibliography style. |
| `templates/literature_review_template.md` | rebuild later: literature | A review opens with its scope and method, closes each theme with a summary, synthesises agreements and debates, and ends with gaps and a research agenda. The literature skill's synthesis takes this structure after its first gate. |
| `templates/policy_brief_template.md` | drop | A policy brief for decision-makers scores options on effectiveness, feasibility, cost and political acceptability and ends with implementation steps and success metrics. The hub produces no policy briefs. |
| `templates/revision_tracking_template.md` | rebuild: rebuttal | One row per reviewer comment with where it changed, and four closing statuses with what each must carry: a location, a justification, the constraint, or an evidence-based rebuttal. With the promise ledger, the letter layout and a completeness checklist, this is the rebuttal skill's core table. |
| `templates/theoretical_paper_template.md` | drop | A theory paper traces a theory's origins, critiques it and proposes an extension with testable propositions. The hub's papers are empirical ML papers, which the conference and IMRaD skeletons cover. |

## academic-paper-reviewer

| File | Mark | Reason |
|---|---|---|
| `SKILL.md` | rebuild: paper-review | A field analysis sets up five independent reviewers, the user confirms the panel, and a synthesis turns the reports into a decision and roadmap; quick and methodology-only modes run smaller panels. Reviewers never edit the manuscript, review materials are untrusted data, a critical devil's-advocate finding forbids Accept, and the revision coaching goes to `rebuttal`; its sprint-contract hard gate waits with `references/sprint_contract_protocol.md`. |
| `agents/field_analyst_agent.md` | rebuild: paper-review | Six dimensions of the paper decide one card per reviewer, each with an identity, three focus points and blind spots, checked for non-overlapping focus and adjusted for theory papers, review papers and first drafts. The target venue is asked of the user whenever the paper does not name one, since `references/top_journals_by_field.md` lists no ML venue. |
| `agents/eic_agent.md` | rebuild: paper-review | The editor weighs originality, significance, venue fit and whether title, abstract, introduction and conclusion agree, and cites three to five strengths and weaknesses. A paper is not marked down for a conclusion the reviewer disagrees with, journal preferences come from the venue the user names, and the sprint-contract section joins `references/sprint_contract_protocol.md` after the first gate. |
| `agents/methodology_reviewer_agent.md` | rebuild: paper-review | Six steps take the methodology review from question alignment to reproducibility, with the statistical checklist of `references/statistical_reporting_standards.md` and a fallacy table that lists overfitting without a holdout and multicollinearity. Its sprint-contract section waits with `references/sprint_contract_protocol.md`. |
| `agents/domain_reviewer_agent.md` | rebuild: paper-review | The domain reviewer checks coverage of the last three to five years, factual accuracy and overclaiming, and holds information-science and AI papers to experiments, recognised benchmarks and state-of-the-art comparison. A severity resting on a field norm needs an external source or is down-rated and marked unverified; the sprint-contract part follows `references/sprint_contract_protocol.md`. |
| `agents/perspective_reviewer_agent.md` | rebuild: paper-review | Hidden assumptions, work from adjacent fields, feasibility, unintended consequences and ethics make up the third reviewer's view, every criticism brings an alternative, and the sprint-contract part is deferred to `references/sprint_contract_protocol.md`. The synthesis counts this reviewer in its panel of four, and on a purely technical paper it may focus on ethics, misuse risk, boundary conditions and the feasibility of technical assumptions. |
| `agents/devils_advocate_reviewer_agent.md` | rebuild: paper-review | The devil's advocate states the strongest counter-argument and raises a critical finding on four grounds, such as data that contradict the conclusion or a simpler rival explanation. A rebuttal scored below four is never conceded, a concession right after another needs a five, and the cross-model option stays behind while the sprint-contract part waits for its protocol. |
| `agents/editorial_synthesizer_agent.md` | rebuild: paper-review | Each weakness is split into sub-claims with every reviewer's position, and consensus is counted over the four reviewers besides the devil's advocate, so silence is never agreement. Disputes are settled by evidence, the roadmap holds no item the reviewers did not raise, and the sprint-contract arithmetic is abandoned. |
| `examples/subclaim_decomposition_example.md` | rebuild: paper-review | In this worked case one "statistics" weakness splits into two sub-claims, each counted over all four reviewers, so two agreeing is corroboration and not consensus. The review skill can use it as a test case for its synthesis. |
| `examples/interdisciplinary_review_example.md` | rebuild: paper-review | Reviewing an ML paper, the methodology reviewer finds temporal leakage from k-fold on a time series and oversampling with twelve positives, two checks its own list lacks. The example also shows arbitration by reviewer confidence and the third reviewer's top finding, with its higher-education framing removed. |
| `examples/hei_paper_review_example.md` | drop | A full review of a qualitative multiple-case study on Taiwanese private universities, from the reviewer cards through the decision and roadmap. It applies the agent rules and the report templates and states no rule of its own; the ML example is the one kept. |
| `references/calibration_mode_protocol.md` | rebuild later: paper-review | Calibration runs the reviewer on the user's papers with known outcomes, five fresh runs each, reports error rates with bootstrap intervals, and shows that profile on later reviews. The review skill's first gate is a blind comparison on one paper, so calibration comes after it. |
| `references/changelog.md` | drop | Versions 1.0 to 1.4 of the reviewer skill are logged here without a single rule. |
| `references/guided_mode_protocol.md` | rebuild later: paper-review | In the guided mode the reviews are revealed in a dialogue, strengths first and the devil's advocate last, 200 to 400 words per turn, with questions rather than verdicts. This alternative presentation is not needed for the first gate. |
| `references/integration_guide.md` | drop | A nine-step example of the full pipeline from research to formatted paper. The order of stages belongs to the researcher's roadmap templates, and the file states no rule of its own. |
| `references/editorial_decision_standards.md` | rebuild: paper-review | Accept, minor, major and reject each have criteria on the 1-to-5 dimension average, and a decision matrix maps the four recommendations, with rules for a split or an outlier. A fatal methodology flaw leans to reject whatever the rest, major rounds stop at two, and the review skill restates these thresholds on the 0-to-100 scale of `references/quality_rubrics.md`. |
| `references/quality_rubrics.md` | rebuild: paper-review | Five weighted dimensions are scored 0 to 100 with descriptors per range, and the weighted score maps to a decision; the review skill keeps this scale. Scores are relative to the target venue and ordinal without calibration, and two dimensions at odds are reported, not averaged. |
| `references/re_review_mode_protocol.md` | rebuild: paper-review | Re-review verifies each priority-one item where the author says, accepts only when every required item is fully addressed, needs answers to 80 percent of priority-two items, and lists new problems. A promise not kept without a rationale is flagged, and the residual coaching after re-review goes to `rebuttal`. |
| `references/review_criteria_framework.md` | rebuild: paper-review | Seven dimensions scored one to five, extra criteria per paper type, and reviewer biases to avoid, such as novelty bias or judging language as quality, guide every reviewer. The review skill keeps the 0-to-100 scale of `references/quality_rubrics.md` and restates these criteria on it. |
| `references/review_quality_thinking.md` | rebuild: paper-review | Three lenses order the reviewer's questions, and each reviewer trap has a fix, such as writing the score first so that mild comments and harsh scores do not diverge. After drafting, five calibration questions are asked, among them whether the paper would mislead readers as published. |
| `references/sprint_contract_protocol.md` | rebuild later: paper-review | Each reviewer writes, before seeing the paper, what will trigger a block or a warning per dimension, then scores against those triggers and may override one in writing. The pre-commitment rule, which the editor and reviewer agents repeat, is worth carrying after the first gate; the JSON contract, the lint and the panel arithmetic are not. |
| `references/statistical_reporting_standards.md` | rebuild: paper-review | The methodology reviewer uses this file for its universal statistical checklist, its completeness score and its red flags, such as only the best of several models reported or text contradicting the tables. The review skill has no use for the per-method checklists from t tests to multilevel models, the higher-education methods or the APA number formats. |
| `references/top_journals_by_field.md` | drop | Journals per field are listed with impact-factor ranges and review preferences, mostly in education, management and social science, and the computer-science list names no ML conference. The venue step stays in `agents/field_analyst_agent.md`, which asks the user for the target venue instead of choosing from this list. |
| `templates/editorial_decision_template.md` | rebuild: paper-review | The decision letter gives the decision, the reviewer summary, consensus and disagreements with the editor's rationale, and why the decision is neither stricter nor milder. Required revisions carry acceptance criteria, and the roadmap carries effort estimates and priorities from severity. |
| `templates/peer_review_report_template.md` | rebuild: paper-review | Each reviewer reports a recommendation and confidence, three to five strengths cited to the paper, and weaknesses as problem, why it matters, suggestion and severity. Section comments and two to four questions whose answers could change the paper follow, with examples of a constructive tone. |
| `templates/revision_response_template.md` | rebuild: rebuttal | Every comment is answered as comment, response, changes made, with page and paragraph pointers, and a disagreement is argued with evidence. Tables of required and suggested revisions and a page-by-page change log complete the letter the rebuttal skill writes. |

## academic-pipeline

| File | Mark | Reason |
|---|---|---|
| `SKILL.md` | rebuild: researcher | The pipeline enters at whatever stage the user's materials allow, runs research, writing, integrity checks, review and revision with at most one re-revision, and keeps the orchestrator from substantive work. The researcher inherits stage order, entry and the checkpoint system; `paper` takes finalisation, `paper-review` the early stop, and nobody the process summary, budget display or collaboration observer. |
| `agents/pipeline_orchestrator_agent.md` | rebuild: researcher | The orchestrator picks the entry stage, passes each stage its declared inputs and holds checkpoints at integrity checks, review decisions and finalisation; its citation finalizer, passport hashing, audit-artifact gate and observer dispatch end with it. The researcher inherits its stage loop, fallback matrix, mode-switch table and mid-entry check, `paper` its patch sequencing and claim-audit gate, `rebuttal` the coaching, `submit-manuscript` the package gate. |
| `agents/state_tracker_agent.md` | rebuild: researcher | A single writer owns the pipeline state, where stage status only advances, materials are versioned and never overwritten, and a missing required material blocks the transition. Because Ordo's plan ledger already keeps state and versions, the researcher adopts only the prerequisite table and the closing audit trail. |
| `agents/integrity_verification_agent.md` | rebuild: paper | The integrity agent looks every reference up rather than trusting memory, ends each as verified, not found or mismatched, and names patterns such as a DOI resolving to another paper. It traces claims to their passages and to the project's own runs and checks numbers for consistency; the paper skill takes all of it, lookup rules included, and the literature skill reuses them. |
| `agents/claim_ref_alignment_audit_agent.md` | rebuild: paper | The audit splits each cited claim into sub-claims and judges them against the retrieved source text, never judging a source it did not read. It keeps paywalls apart from transient outages and flags uncited empirical sentences unless the project's own run backs them, with the passport format, cache keys and versioning dropped. |
| `agents/collaboration_depth_agent.md` | drop | An advisory observer scores how the user works with the assistant (what is handed over, what is checked, where freed time goes) against a rubric from an education study. That grades the person rather than the research, which none of the new skills does. |
| `examples/full_pipeline_example.md` | drop | A first-time Master's student on Taiwanese university enrolment goes through writing, review, one revision, re-review and format conversion, with four reviewers and no integrity check at either gate. Skipping those checks breaks rules that `SKILL.md` sets, so the researcher cannot use the run as a model. |
| `examples/integrity_failure_recovery.md` | rebuild: paper | An integrity check fails on a fabricated reference, a number from the wrong column of the right table, and wrong years and initials, and only the corrected items are rechecked. Two rules here are missing from the integrity agent: a fabricated reference is replaced by a read source with every citing sentence rewritten to it, and a corrected number names its page and table. |
| `examples/mid_entry_example.md` | drop | Starting from a finished ML paper, this run goes through a quick review, a revision round, a full re-review and LaTeX output, and skips the integrity check that `SKILL.md` requires. Entry from existing materials is already covered in `SKILL.md` and the orchestrator. |
| `references/adapters/overview.md` | drop | Programs that turn a reference library (Zotero, Obsidian, a folder of PDFs) into the passport's corpus file must log rejected entries, give deterministic output and record provenance. The literature skill reads the `.bib` kept beside each hub manuscript directly and needs no conversion layer. |
| `references/adapters/.gitkeep` | drop | This empty placeholder only keeps the adapters folder in git and holds no content. |
| `references/ai_research_failure_modes.md` | rebuild: paper | Seven failure modes of AI-assisted research come with detection questions, among them a result no run produced, a learned shortcut, a bug written up as a surprise, and Methods that differ from the run config. Every number is traced to a saved run and config, a mode lacking its logs blocks, and the paper skill needs this for ML papers. |
| `references/changelog.md` | drop | The file is the pipeline skill's version history, with nothing for the new skills. |
| `references/claim_audit_calibration_protocol.md` | rebuild later: paper | The claim-faithfulness judge is calibrated on gold claim and source pairs, with error rates per verdict class against fixed thresholds and a miss rate for compound claims it stops splitting. Measuring the judge is not needed for the paper skill's first gate, just as the review skill calibrates later. |
| `references/claim_verification_protocol.md` | rebuild: paper | The integrity agent's Phase E repeats this file's claim scope, three steps, five verdicts and sampling, leaving only its example column, such as "declined sharply" for a 2.1 percent fall, unique to it. The paper skill merges it with `agents/integrity_verification_agent.md` into one integrity page, where that column illustrates the verdicts. |
| `references/integrity_review_protocol.md` | drop | This summary gives a sampled check before review, a fuller check after revision and a three-round cap on corrections. `agents/integrity_verification_agent.md` states both modes and the cap in full. |
| `references/external_review_protocol.md` | rebuild: rebuttal | The user confirms real reviewer comments parsed into items, then works through each major comment with the assistant as agree, partly agree or disagree, and accepts everything only by skipping that discussion. Its closing check, that every comment is answered by a change present in the draft, becomes the rebuttal skill's completeness check. |
| `references/literature_corpus_consumers.md` | rebuild: literature | Searching starts from the user's own library, screened under the same criteria as external results, with every skipped entry given a reason and the library never modified; external search then fills only the gaps. For the literature skill that library is the project's `.bib`, and the passport block format has no role. |
| `references/mode_advisor.md` | rebuild: researcher | Matching skills and modes to the user's situation, the advisor lists what each skill cannot do and the materials each entry point requires, such as a verified draft with its integrity report before review. Its entry table becomes the input each researcher stage starts from, and each new skill's Use instead section gets the routing. |
| `references/passport_as_reset_boundary.md` | drop | With `ARS_PASSPORT_RESET=1` set, sessions end at each full checkpoint and resume in a fresh one from a hashed, append-only passport ledger, locked against a double resume. Every Ordo plan step already resumes from the ledger and state file on disk, and roadmap entry 13 gives researcher stages written input files. |
| `references/pipeline_state_machine.md` | rebuild: researcher | Review may not precede the integrity check, finalisation needs the final check, and review does not return after the second revision; each stage lists its required material and what a missing one triggers. The roadmap templates of entry 13 encode this order and these prerequisites as dependencies, without the passport reset transitions. |
| `references/plagiarism_detection_protocol.md` | rebuild: paper | The paper skill's integrity check runs this screen at both gates: sampled sentences searched on the web and graded from original to verbatim, and a self-plagiarism check against the authors' earlier papers. Its six AI-writing indicators alert without deciding and suit the `writing` skill's style rules. |
| `references/process_summary_protocol.md` | drop | At the end a PDF record of the session scores the user's collaboration from 1 to 100 and adds an AI self-reflection built on concession rates. The plan ledger already records each decision, finding and usage row, and no new skill scores the user. |
| `references/progress_dashboard_template.md` | drop | Asked for status, the pipeline prints a box of stages with integrity and review history. The researcher's state is its roadmap and plan ledger, which `/roadmap` and `plan-help` already show. |
| `references/reinforcement_content.md` | drop | At each stage transition one rule and one anti-pattern are repeated, such as "a gray zone is a fail". Each rule is stated in the file that owns it (`agents/integrity_verification_agent.md`, `agents/pipeline_orchestrator_agent.md`, `references/pipeline_state_machine.md`, the academic-paper and reviewer skills), and an Ordo skill loads its rules at every step. |
| `references/reproducibility_audit.md` | drop | This file restates the mandatory integrity checks, fixed reviewer roles and explicit pass criteria, then prints an audit trail at the end. `SKILL.md` and the agents state the guarantees, and the trail goes with `agents/state_tracker_agent.md` to the researcher. |
| `references/score_trajectory_protocol.md` | rebuild: paper-review | Re-review compares each rubric dimension with the first review: a drop of more than three points out of 100 stops for the user, and an overall gain under three with no blocking issue suggests stopping. The re-review mode named in roadmap entry 6 takes these rules. |
| `references/two_stage_review_protocol.md` | drop | The file outlines a first review and a verification re-review with coaching between them, each pointing to `academic-paper-reviewer/SKILL.md`. That skill and `references/pipeline_state_machine.md` state the flow, while `agents/pipeline_orchestrator_agent.md` and the reviewer's `references/re_review_mode_protocol.md` hold the coaching. |
| `references/team_collaboration_protocol.md` | drop | For a research team the file sets roles, handoff checklists, git branches and tags per stage, who settles which disagreement, and message templates. One researcher runs the hub with git history as the version record, and no new skill has a team layer. |
| `templates/pipeline_status_template.md` | drop | This fill-in form renders the status box and a one-line progress bar with next-step suggestions. It is the output of `references/progress_dashboard_template.md` and is dropped together with that file. |
