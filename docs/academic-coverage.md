# Coverage of the academic skills

For each academic skill installed in research-hub that has a section below, this list says what happens to each of its files when Ordo's own writing skills replace the skill (roadmap entries 3 to 16). Each file is marked with the new skill that must cover what it does, the new skill that will cover it later, or `drop`, and the reason says what the file holds that decides it. A new skill's plan starts from the rows that name it.

The files are those in `research-hub/.agents/skills/` of the skills `academic-paper`, `academic-paper-reviewer`, `academic-pipeline` and `deep-research`, installed from `imbad0202/academic-research-skills`. Their `computedHash` values in research-hub's `skills-lock.json` when this list was written:

- `academic-paper`: `54cd6f3549239f10821d688f0b2894c165b8ee3162fb235711f1223e08973f8b`
- `academic-paper-reviewer`: `6940aa353c2ad3ff52082a33487e4a98fd6813aed6d25929f4f82a499c21b9fd`
- `academic-pipeline`: `04dea9ef49eae79fd2f4ce6a04765f15ee7afe32b79a9bdcb72cf7cfc54fea38`
- `deep-research`: `e20235bd776f36c9e8cceb38fe71713ad008695d92e91f7869a3c6c8cba964ad`

The marks:

- `rebuild: <skill>`: the new skill that roadmap entry builds must cover what the file does before that entry's gate.
- `rebuild later: <skill>`: the file belongs to that new skill, but the skill's first gate does not need it.
- `drop`: no new skill needs the file; the reason says why, and where a rule in it is kept elsewhere, it names the file that keeps it.

Every file was read in full before it was marked. A file whose content splits across skills is marked for the skill that takes most of it, and the reason names where the rest goes.

The check, run from the repository root over the skills that have a section below, exits 0 only when every file of the named skills is listed exactly once with a valid mark and a reason:

```sh
python3 utils/check_coverage.py docs/academic-coverage.md /Users/axelfaes/workspace/research-hub/.agents/skills academic-paper
```

## New skills

| Skill | Roadmap entry |
|---|---|
| writing | 3 |
| code-comments | 4 |
| paper | 5 |
| paper-review | 6 |
| rebuttal | 7 |
| grant | 8 |
| literature | 9 |
| idea | 10 |
| scaffold | 11 |
| project-docs | 12 |
| researcher | 13 |
| submit-manuscript | 14 |
| submit-grant | 15 |

## academic-paper

| File | Mark | Reason |
|---|---|---|
| `SKILL.md` | rebuild: paper | The entry point: eleven modes, the eight-phase flow with its checkpoints (the outline approved before the argument, at most two revision rounds), and the quality standards (every claim cited, no orphan citations). The paper skill takes these, `rebuttal` the revision-coach and rebuttal-audit modes; the Chinese triggers, higher-education defaults and generator-evaluator machinery are not carried. |
| `agents/abstract_bilingual_agent.md` | rebuild later: paper | The abstract rules that are language-neutral: five parts (background, purpose, method, findings, implications), 150 to 300 words, five to seven keywords that do not repeat the title, no citations, every abbreviation defined. Most of the file is the independent zh-TW abstract, which is not carried; the abstract rules are not needed for the paper skill's first gate, a revision round. |
| `agents/argument_builder_agent.md` | rebuild: paper | The argument blueprint: a thesis, three to five claim-evidence-reasoning chains, and the strongest counter-argument for each with a rebuttal. Drafting stops when two weak-argument signs appear, such as circular reasoning, correlation taken as causation or an undefined key term. |
| `agents/citation_compliance_agent.md` | rebuild: paper | The citation audit: citations and reference entries cross-checked both ways for orphans and mismatches, DOI form, self-citation, source age and retractions, with format fixed automatically and a claim the source lacks stopped. It is the source of the paper skill's `\cite`-against-`.bib` and DOI checks; the Chinese citation rules are not carried. |
| `agents/draft_writer_agent.md` | rebuild: paper | Section-by-section drafting against the outline, the topic-evidence-explanation-link paragraph, the rule that a cited source predates the event it evidences, and revision as hash-checked block patches, which `tools/manuscript`'s `anchorize_tex.py` and `apply_tex_patch.py` already do for LaTeX. The generator-evaluator phases, the claim intent manifest and the HTML-comment citation layers are not carried. |
| `agents/formatter_agent.md` | rebuild later: paper | Output conversion to LaTeX with a `.bib` or to DOCX, under the rule that formatting never changes content (word count within 1 percent), plus journal-specific formatting profiles. The papers start from the venue's LaTeX template, so the first gate does not need it; the cover letter goes to `submit-manuscript`, and the provenance gates and zh-TW fonts are not carried. |
| `agents/intake_agent.md` | rebuild: paper | The configuration interview that fixes paper type, venue, citation format, word count, materials, co-authors and funding before writing, with venue limits recorded only as the scholar states them. The paper skill keeps those limits in the project until roadmap entry 13's `venues/` files hold them; the zh-TW options, style calibration and evidence-profile machinery are not carried. |
| `agents/literature_strategist_agent.md` | rebuild: literature | The search strategy: Boolean strings, backward and forward citation chaining and semantic search, criteria fixed beforehand, title-then-full-text screening, a source-by-theme matrix, gaps and saturation stopping rules. The Taiwan databases, the passport corpus protocol and the trust-chain fields are not carried. |
| `agents/peer_reviewer_agent.md` | rebuild: paper-review | The single-reviewer rubric: five weighted dimensions, a verdict from the weighted score, per-section issues with severity and fix, and checks that title, abstract and conclusion match the findings. Its re-review checks only the flagged items and what the revision broke, for at most two rounds; the generator-evaluator phases are not carried. |
| `agents/revision_coach_agent.md` | rebuild: rebuttal | Reviewer comments parsed into items with none dropped: compound comments split, each classified and mapped to a section, and each promise extracted with the evidence that would fulfil it. The parse is shown to the user before the response is written, and this is the rebuttal skill's point table. |
| `agents/socratic_mentor_agent.md` | rebuild later: paper | The paper-planning dialogue: a readiness check, thesis crystallisation, five questions per chapter, a four-type question taxonomy, and convergence signals with a cap on rounds. It comes after the paper skill's first gate, a revision round, and the taxonomy also serves the `idea` interview; the commitment gates and wording-pattern list are not carried. |
| `agents/structure_architect_agent.md` | rebuild: paper | The outline step: a structure chosen from the paper type, a purpose and word count per section within 5 percent of the total, every source mapped to a section, and a transition at every boundary. The user approves the outline before the argument is built. |
| `agents/visualization_agent.md` | rebuild: paper | Figures: a chart-type decision tree, 300 dpi sizes, a colourblind-safe palette, the pitfalls (dual y-axes, truncated axes, missing error bars), LaTeX figure templates, and ten checks per figure including that plotted values match the data. Each figure is traced to its data and script, and roadmap entry 5 names figures in the paper skill's goal. |
| `examples/chinese_paper_example.md` | drop | A complete zh-TW IMRaD paper on Taiwanese private-university enrolment, with fictional data and references, showing the Chinese APA format. Neither the language nor the field is used; the new skills' examples come from real papers in the hub. |
| `examples/clinical_citation_verification_checklist.md` | rebuild: paper | A field-neutral claim-to-source checklist under a clinical example: the reference resolves, the source holds the exact number at a page, table or quote, population and metric match, and limitations are kept. It is the manual form of the paper skill's citation check; the clinical safety note is not carried. |
| `examples/clinical_epistemic_status_example.md` | rebuild: paper | The silent-upgrade audit: a draft may not state more than its source, such as association turned into cause, a dropped hedge, a wider population or a stronger verb. The rule is field-neutral and joins the paper skill's claim checks; the clinical wording is not carried. |
| `examples/commitment_ledger_example.md` | rebuild: rebuttal | Each reviewer promise on an ML paper (a CIFAR-100 ablation, error bars over five seeds) tracked to fulfilled, partial or not fulfilled. A promise not kept needs one of three rationales, and a missing one is flagged; this is the rebuttal skill's trace from point to change. |
| `examples/imrad_hei_example.md` | drop | A complete IMRaD paper on AI-assisted accreditation in Taiwanese higher education with illustrative references and a zh-TW abstract. The section shapes it shows are stated in `references/paper_structure_patterns.md`; a worked example for the paper skill comes from an ML paper in the hub. |
| `examples/literature_review_example.md` | drop | A complete thematic literature review on enrolment decline in East Asia with illustrative references. Its structure (themes, cross-cutting synthesis, gaps, agenda) is stated in `templates/literature_review_template.md` and the structure patterns; the example itself adds no rule. |
| `examples/plan_mode_guided_writing.md` | drop | A full plan-mode dialogue on a blended-learning paper, from the readiness check through the chapter plan. It applies the rules of `agents/socratic_mentor_agent.md` and states none of its own. |
| `examples/revision_mode_example.md` | rebuild: rebuttal | A worked revision round: comments parsed into major and minor items with their sections, the item that changes other sections done first, and a before-and-after table with page pointers. Its response letter, answering each comment with where it changed, is the shape of the rebuttal skill's output. |
| `examples/revision_recovery_example.md` | rebuild: rebuttal | A round in which one reviewer point is declined: it needs a principled reason, a line in the limitations and an explanation in the response. Each response follows comment, response, changes made, which the re-review checks item by item; these are the rebuttal skill's rules for declined points. |
| `examples/version_family_reconciliation_example.md` | rebuild later: literature | A worked case of one work in two versions (arXiv v1 and the NeurIPS proceedings): a sentence that quotes one version must cite that version, and the three acceptable fixes. It concerns which `.bib` entry a claim cites, common in ML where preprints and proceedings coexist, but is not needed for the literature skill's first gate. |
| `references/abstract_writing_guide.md` | rebuild later: paper | Abstract types (structured, unstructured, the extended conference abstract), word counts per venue kind, the five components with patterns and what to avoid in each, and keyword rules. It is the reference behind `agents/abstract_bilingual_agent.md` and repeats its checklist, so the two become one page; the zh-TW half is not carried. |
| `references/academic_writing_style.md` | rebuild: writing | Prose rules for every writing skill: terms defined on first use, clear antecedents, wordy-to-concise and vague-to-precise tables, a hedging scale with when not to hedge, and the tense per section. The register table is kept for engineering and computer science; the zh-TW conventions are not carried. |
| `references/anti_leakage_protocol.md` | rebuild: paper | Every claim, citation, number and method step in a draft comes from the project's own materials, and the methods describe only what the project documents. A topic the materials miss is flagged as a gap, filled from the model only when the user allows it and then tagged; for code projects the methods follow the code and logs. |
| `references/apa7_chinese_citation_guide.md` | drop | Chinese-language APA 7 reference forms, mixed Chinese-English ordering and the citation conventions of Taiwanese TSSCI education journals. No paper in the hub cites in Chinese. |
| `references/apa7_extended_guide.md` | rebuild: paper | The statistics reporting rules no other file of the skill states: exact p values unless below .001, an effect size always reported, the 95 percent CI format and decimal places. The paper skill's statistics page takes them; the APA page rules (headings, title page, reference forms, bias-free language) are not carried, since the venue template sets the style. |
| `references/changelog.md` | drop | The upstream skill's version history, versions 1.0 to 2.5. It records what changed and states no rule. |
| `references/citation_format_switcher.md` | drop | Hand conversion between APA, Chicago, MLA, IEEE and Vancouver, with the in-text and reference-list form of each. The papers are LaTeX with BibTeX, where the venue's bibliography style renders every entry, so no citation is converted by hand; the zh-TW conversion notes are not used either. |
| `references/credit_authorship_guide.md` | rebuild: paper | The fourteen CRediT roles with a lead-or-supporting matrix, the four ICMJE authorship conditions, what goes to the acknowledgments, and the rule that an AI tool is never an author. Roadmap entry 5 names disclosure statements in the paper skill's goal; the higher-education examples are not carried. |
| `references/disclosure_mode_protocol.md` | rebuild: paper | The AI-use disclosure: each category of use marked used, not used or uncertain, with the user confirming the uncertain ones. The venue's phrasing and placement are applied with the policy's access date, an unknown venue's policy is asked for, never guessed, and the policy-anchor rendering is not carried. |
| `references/domain_evidence_profiles.md` | rebuild: literature | Evidence profiles per field, of which the `cs_ml` one applies: archival preprints and conference proceedings count as evidence beside journals, and the risks to surface are non-reproducible results and benchmark cherry-picking. The literature skill takes this as its default for ML work; the profile machinery and the other profiles are not carried. |
| `references/failure_paths.md` | rebuild later: paper | Twelve failure paths, three with rules stated nowhere else: desk rejections by cause, the conference-to-journal extension (30 to 50 percent new content), and a draft far off length handled by listing sections, never by deleting. None is needed for the paper skill's first gate, and the venue switch after a scope rejection will use the venue entries. |
| `references/funding_statement_guide.md` | rebuild: paper | The funding and interest statements: forms for one, several, no or partial funding with the funder's role, a funder's disclaimer quoted verbatim, and a conflict-of-interest statement that is always present. Roadmap entry 5 names these statements; the Taiwanese agency formats are not carried, and each funder's acknowledgement text goes to the grant skill's config. |
| `references/hei_domain_glossary.md` | drop | An English and romanised-Chinese glossary of Taiwanese higher-education terms (institution types, accreditation, governance, enrolment, HEEACT domains). Neither the field nor the language is used. |
| `references/journal_submission_guide.md` | rebuild: submit-manuscript | The submission package: a pre-submission checklist (scope, predatory-journal check, anonymisation, line numbers), the required components, and a cover letter with suggested and excluded reviewers. The response-letter part goes to `rebuttal`, the data-availability templates to the paper skill's statements, and the higher-education journal tables are not carried. |
| `references/latex_template_reference.md` | rebuild later: paper | LaTeX output: an `article` template, the `apa7` class template with the justification override and the `\tabcolsep` column-width formula, BibTeX entry forms per type, natbib and biblatex citation commands, compile fixes and Pandoc commands. The papers start from the venue's own template, so this is not needed for the first gate; the BibTeX entry forms go with the literature skill's `.bib` writing, and the zh-TW settings are not carried. |
| `references/mode_selection_guide.md` | rebuild: paper | Which mode fits what the user has, and the gate for turning a plan into a full draft (each chapter's argument adequate, the question fixed, five seed references). The paper skill's Quick start and Use instead take the routing, pointing to `rebuttal` and `literature`; the zh-TW trigger rows are not carried. |
| `references/paper_structure_patterns.md` | rebuild: paper | Six paper structures (IMRaD, thematic review, theoretical, case study, policy brief, conference paper), each with its section skeleton and word allocation, and a selection table. The paper skill needs the conference and IMRaD skeletons, which for ML papers become introduction, related work, method, experiments, results, discussion, conclusion; the case-study and policy-brief patterns are not carried. |
| `references/plan_mode_protocol.md` | rebuild later: paper | The plan-mode activation signals and the default to plan mode when a request is unclear; the flow itself restates `agents/socratic_mentor_agent.md`, and most signals are also in `SKILL.md` and `references/mode_selection_guide.md`. One signal is stated only here (results in hand, no idea how to shape them into a paper), so the file goes with the planning dialogue, which the first gate does not need. |
| `references/policy_anchor_disclosure_protocol.md` | rebuild: paper | Two venue-neutral disclosure rules: when to write a no-AI statement and when to write nothing, and AI-quoted text attributed but never cited as a primary source. The paper skill's disclosure page takes them, its venue entries take the copyediting carve-out that differs by venue, and the four-anchor rendering is not carried. |
| `references/policy_anchor_table.md` | drop | The verbatim sixteen-field policy matrix for PRISMA-trAIce, ICMJE, Nature and IEEE, with snapshot hashes, behind the policy-anchor rendering, which is not carried. The paper skill's venue entries for Nature and IEEE, including the copyediting carve-out and IEEE's acknowledgments placement, are written from each policy's own page with its access date. |
| `references/revision_patch_protocol.md` | rebuild: paper | The revision round as anchorize, patch, apply: block ids never renumbered, one stale hash rejects the whole patch with the base untouched, a structural change refused unless acknowledged, and never a hand-edited patch. This is what `tools/manuscript`'s `anchorize_tex.py` and `apply_tex_patch.py` do, which roadmap entry 5 moves into the paper skill. |
| `references/statistical_visualization_standards.md` | rebuild: paper | The reference behind the figure rules: when not to draw a figure, subtle errors such as unequal histogram bins, palettes with hex codes, matplotlib and ggplot2 templates, and LaTeX figure environments. It and `agents/visualization_agent.md` become one figures page in the paper skill. |
| `references/venue_disclosure_policies.md` | rebuild: paper | The AI-use policy of ICLR, NeurIPS, Nature, Science, ACL and EMNLP with source URL, access date, required phrasing and placement, and the rule that an unlisted venue's policy is pasted by the user, never guessed. The disclosure step needs it at roadmap entry 5, so the paper skill holds these entries until entry 13 moves them into the researcher's `venues/` files. |
| `references/vlm_figure_verification.md` | rebuild: paper | Two figure checks: the rendered image read by a vision model against its data, and the trace from each figure to its data and script with every citing claim checked. The trace belongs to the paper skill's figures page; the vision check can come later. |
| `references/workflow_phase_details.md` | drop | A per-phase summary of the eight phases, their agents, outputs and checkpoints. Every item is stated in `SKILL.md` and the agent files. |
| `references/writing_judgment_framework.md` | rebuild: writing | Judgment rules for prose: the clarity test of removing a paragraph, the four questions a reader must be able to answer, the voice each discipline trusts, and the "so what" of each section. The revision decision matrix at the end goes to `rebuttal`. |
| `references/writing_quality_check.md` | rebuild: writing | The anti-pattern list the writing base checks: filler terms, at most three dashes per paper, throat-clearing openers, forced groups of three, synonym cycling, binary contrasts and five equal-length sentences in a row. Roadmap entry 3 names the dash and anti-pattern checks. |
| `templates/bilingual_abstract_template.md` | drop | A fill-in template pairing an English and a zh-TW abstract with an independence checklist. The English half repeats the five parts of `references/abstract_writing_guide.md`; the zh-TW half is not used. |
| `templates/case_study_template.md` | drop | A case-study paper skeleton (case selection, case description with a timeline, themes, transferability, lessons for practitioners and policymakers). No paper in the hub is a case study. |
| `templates/conference_paper_template.md` | rebuild: paper | The conference paper skeleton: problem and motivation, the research question, a contribution paragraph that states specifically what is new, related work ending in the gap, method, results per finding, limitations, conclusion with future work, acknowledgments. Most papers in the hub are conference papers, so this is the paper skill's default skeleton, adapted to the venue template. |
| `templates/credit_statement_template.md` | rebuild: paper | The CRediT form: an author table with ORCID and the corresponding author, the lead-or-supporting matrix, the statement, an equal-contribution sentence, and a checklist with the note that all co-authors agree the roles. It and `references/credit_authorship_guide.md` become one statements page; the zh-TW form is not carried. |
| `templates/funding_statement_template.md` | rebuild: paper | The funding and interest form: a registry of funding sources, the funder's role in design and publication, the statement per case, and funder formats including ERC and Horizon Europe. It and `references/funding_statement_guide.md` become one statements page; the Taiwanese formats are not carried. |
| `templates/imrad_template.md` | rebuild later: paper | The IMRaD journal skeleton with its subsections (context, problem, gap, questions, significance; framework and themes; design, sample, data, measures, analysis, validity, ethics; results per question; discussion with limitations and future work). The conference skeleton comes first; this one serves a journal version of a paper. |
| `templates/latex_article_template.tex` | drop | A generic APA-style `article` template (double spacing, Times, natbib `apalike`, a commented zh-TW abstract). The papers start from the venue's own class and style file, which fix spacing, fonts and the bibliography style. |
| `templates/literature_review_template.md` | rebuild later: literature | The review skeleton: scope, the review method, themes each closed by a summary, a synthesis of agreements and debates, then gaps and a research agenda. The literature skill's synthesis takes this structure after its first gate. |
| `templates/policy_brief_template.md` | drop | A policy brief for decision-makers (executive summary, options scored on effectiveness, feasibility, cost and political acceptability, implementation steps, success metrics). No output in the hub is a policy brief. |
| `templates/revision_tracking_template.md` | rebuild: rebuttal | One row per reviewer comment with where it changed, and four closing statuses with what each must carry: a location, a justification, the constraint, or an evidence-based rebuttal. With the promise ledger, the letter layout and a completeness checklist, this is the rebuttal skill's core table. |
| `templates/theoretical_paper_template.md` | drop | A skeleton for a paper that develops or critiques a theory (origins, critical analysis, proposed extension with testable propositions). The hub's papers are empirical ML papers; the conference and IMRaD skeletons cover them. |
